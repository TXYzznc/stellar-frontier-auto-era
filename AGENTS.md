# AI Friendly Frame — Codex 入口

Unity 2022.3.62f3 + GF_X 的领域无关项目框架。当前基线不包含业务代码、
内容资产、产品规格、演示场景、示例数据或教学样例。

Claude Code 的等价入口为 [.claude/CLAUDE.md](./.claude/CLAUDE.md)。

本仓库实例已经进入“星际拓荒：自动纪元”产品开发阶段。正式设计来源、
第一版范围、任务表权限和框架/产品代码边界统一从
[项目开发基线](./Docs/Development/ProjectBaseline.md)进入。

## AI 使用统计（已关闭）

本项目使用统计已由用户明确关闭（2026-09-12），对应采集 hook 已移除。新会话直接执行用户任务，不运行首次激活检查、不请求启用、不运行 init 或修改 Hook 信任。除非用户再次明确要求启用，否则保持关闭。历史统计保留。

## Source of truth

- `.claude/agents/*.md` 是 agent 配置源。
- `.codex/agents/*.toml` 由 `python tools/sync-agents.py` 生成，禁止直接编辑。
- `.claude/skills/<name>/SKILL.md` 是项目 SKILL 的唯一来源。
- `.claude/SKILL_MATRIX.md` 定义 agent↔SKILL 推荐清单，不限制角色使用其他可用技能。
- `python tools/sync-agents.py` 同步原生 Codex 角色配置及本机 `.agents/skills` 发现链接；默认继承宿主模型。
- `openspec/` 管理中大型结构化变更。

## Codex 执行语义

匹配专业职责时先读取对应 `.codex/agents/<name>.toml`。只有用户明确要求
子 agent、委派或并行 agent 时才使用 sub-agent；否则主对话按对应职责和
推荐能力等价执行；长期窗口与子Agent均不受技能白名单限制，仍受任务授权和写入范围约束。

轻量的读取、解释和局部修复可以由主对话直接处理。跨职能任务由主对话编排。

## 多窗口高自治协作

所有角色每次对用户回复（含进度与最终回复）开头，必须简短列出本角色全部未完成任务：处理中、
待处理／阻塞／挂起、排队及实际执行顺序；空项写“无”，不可遗漏Pending或将阻塞伪称运行。
遵循 [任务概览规则](./Docs/Development/Dispatch/TaskQueue.md)，同回合无变化可复用已核实快照。
任务概览不构成暂停、抢占或启动新工作的授权。

长期专业窗口在已派发任务、可写路径、已确认方案和验收边界内自主讨论、实施、验证和增量归档，
无需制作人逐步放行。专业窗口可以直接询问用户；用户决定由提问窗口记录，并点对点同步受影响
窗口。普通进度、单项决策、测试通过、增量归档和独立提交不通知制作人。

通知与执行必须解耦：不需要用户或制作人作出决定、补充授权、解除资源冲突或承担明确动作时，
专业窗口不得暂停，也不得等待“已阅”回复。普通进度和可自行观察的信息无需专门通知用户；确有
中央登记、跨窗口知会或风险留痕需要时，可以非阻塞通知制作人或相关窗口，但发送后必须继续执行
所有安全且在范围内的后续工作。只有缺少决定／授权会使下一步无法安全执行时才进入等待。
自然阶段边界、一次回复结束、完成中间产物或写出“下一步”均不构成暂停条件；只要派发任务尚未
完成且下一步仍安全、已授权、无真实资源冲突，窗口必须在后续回合中自动继续推进。
窗口声称仍在工作时必须保持当前任务为运行状态，不得结束回合、显示空闲后再宣称“后台继续”。
长耗时工具调用、编译、渲染和跨窗口短时协调应在同一执行回合内等待并定期给出简短进度；只有
派发范围完成，或出现确实需要外部决定且无法继续其它安全工作的阻塞时，才允许结束该执行回合。
每个长期窗口同时只能有一个Active任务。独立新任务写入`.ai/dispatch/task-queue.local.json`的
Pending队列，按明确优先级和同级FIFO执行；不得向运行中窗口发送普通新任务消息。只有S0／S1或
资源安全风险、用户明确要求立即切换、当前产物已确定作废时，才可在安全检查点显式抢占。

专业窗口在每个已冻结的独立工作单元开始前，必须执行一次“快速执行候选检查”。同时满足
`RapidExecution.md`六项门槛的批量配置、机械装配、小范围固定实现、扫描、测试重跑和证据整理，
默认由原负责人直接写入`rapid-executor`队列，不经过制作人；原负责人保留专业复核和缺陷接收责任。
只有拆包／交接／复核成本不低于自行完成、任务与当前独占工具现场不可安全分离，或工作单元包含
尚未冻结的专业判断时，原窗口才自行执行。快速执行入队与回传均不得打断原窗口的Active任务。

所有Git暂存和提交仅在用户明确手动触发后交给`AutoEra｜Git集成`窗口；不得自动请求、入队或唤醒。遵守
[Git集成工作流](./Docs/Development/Dispatch/GitIntegration.md)；专业窗口不自行操作共享索引。
制作人只处理范围／优先级／依赖变化、点对点无法解决的跨职能冲突、共享资源异常、新授权、
仓库异常和整个派发批次验收。完整规则见
[多窗口任务派发](./Docs/Development/Dispatch/README.md)。

## 决策门槛

讨论新增对象时，先核对其所属通用类别、已有系统合同和最新决策，采用“继承规则＋差异项”的方式。
已由建筑、机器、资源、物流等通用规则决定的行为直接引用，不拆成新问题反复请求确认，也不按对象
复制一套独立规则。只有确实新增、存在冲突、需要例外或尚无依据的差异才提问；讨论不能靠重复
已确认事项或询问既有默认行为凑数。文档优先引用权威来源，避免多处复制造成失步。

检测到设计、架构、重构、大改、重写、PRD、系统、范式、方案或思路时：

1. 先使用 `grill-me` 或 `grill-with-docs` 澄清目标、边界、验收和约束；
2. 按风险补齐目标、边界、验收与约束；已确认事项直接复用，不设最低提问轮数；
3. 有实质决策时增量归档；纯分析不擅自改文档，已授权且无待决事项时直接执行；
4. 评估规模，命中中大型变更时建立 OpenSpec change；
5. 只有共识冲突、不可逆决策或触及框架核心时才中断执行。

## 路由

| 任务 | Agent |
|---|---|
| 范围、计划、排期、风险、研究 | `producer` |
| Unity 架构、模块边界、性能预算 | `client-lead` |
| Unity C#、输入、序列化、物理、UI 技术接入 | `client-unity` |
| Shader、渲染、光照和 TA | `client-ta` |
| 服务端与网络架构 | `net-lead` |
| API、认证、缓存和消息系统 | `net-backend` |
| 数据库、索引和迁移 | `net-db` |
| 测试、缺陷分析和质量验证 | `qa-engineer` |
| 构建、CI/CD 和发布 | `devops-engineer` |
| Editor、内部工具、代码生成和 SKILL | `tools-engineer` |
| 视觉方向与资源评审 | `art-director` |
| UI、字体、2D、3D、动画、VFX 资源技术 | 对应 `art-*` |
| 已冻结、低风险、可自动验收的短执行包 | `AutoEra｜快速执行`长期窗口（`gpt-5.3-codex-spark`） |

不存在常驻的玩法、内容或产品领域 agent。具体项目需要这些能力时，在项目层
另行添加。

快速执行窗口不是专业领域Agent，不参与需求、架构、美术或OpenSpec核心决策。只有任务已明确
目标、允许／禁止路径、冻结依据、自动门禁与结果接收负责人时才能领取；完整规则见
[快速实施与验证职能](./Docs/Development/Dispatch/RapidExecution.md)。

## 项目约束

- 平台：Windows 10。
- Unity：2022.3.62f3；所有实现、工作流与参考资料均以 Unity 2022.3 API 为准。
- 框架核心：`Assets/Game/ScriptsBuiltin/`。
- 通用扩展与项目接入：`Assets/Game/Scripts/`；自动纪元产品代码只能进入`Assets/Game/Scripts/AutoEra/`并使用`AutoEra.*`命名空间。
- 代码结构查询优先使用 codebase-memory；不可用时用最小范围 `rg`。
- 修改 Unity 代码前先读 [.claude/conventions.md](./.claude/conventions.md)。
- Editor Play Mode 内的小范围 C# 方法体迭代可使用 FSR；配置、适用范围和验收见 [Docs/Development/FastScriptReload.md](./Docs/Development/FastScriptReload.md)。结构变更、泛型、字段/序列化、程序集和依赖变更必须停止 Play Mode 后按普通 Unity 编译流程验证。
- FSR 只用于 Editor Play Mode 开发效率；不启用 `LiveScriptReload_IncludeInBuild_Enabled`，不将其当作已发布 Player 的热更新方案，也不额外安装第二份 FSR。
- 不在高频循环制造 GC 分配。
- ScriptableObject 是配置载体，不是运行时数据库。
- 输入必须经过框架输入抽象。
- 不在框架基线提交业务生成物、演示资源或固定项目路径。
- 所有Git提交信息必须使用中文，包括提交标题、概要和正文描述；专业术语、文件名、类型名与
  change ID可保留原文。历史提交不因此重写。

## 框架纯度

运行：

```powershell
python tools/audit_framework_purity.py
```

审计必须覆盖 agent↔SKILL、OpenSpec、禁止内容、业务生成物和固定路径。
具体项目的领域内容应存在于项目自己的变更中，不得回写框架基线。
