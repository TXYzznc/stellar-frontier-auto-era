---
description: AI 工具使用统计的首次对话安全激活
alwaysApply: true
---

每个新 Agent 会话第一次处理用户任务前，先只读执行 `python tools/log_tool_usage.py doctor --editor cursor --json`。

若 `editors.cursor.active` 为 true，继续原任务且本会话不再检查。若为 false，暂停原任务，告诉用户本工具只在本地记录 Tool / SKILL / Agent / MCP 名称和会话元数据，不记录 Prompt、代码、参数、完整命令或完整路径，并询问是否启用。未明确确认前不得运行 init 或继续原任务。

确认后执行 `python tools/log_tool_usage.py init --editor cursor --yes`，要求用户将仓库设为 Trusted Workspace、按 Cursor 提示允许项目 Hook，然后重开会话并重新发送原任务。本轮不继续被暂停的任务。脚本不存在时直接继续原任务。
