# ART-006 美术视觉 Prefab 候选

本目录只保存 ART-006 的美术候选，不包含 GF UIForm 生命周期、C#、业务状态绑定或资源加载键。

- `*_V01.prefab`：由浅层结构原型创建的首轮变体。经层级核验，未包含中枢五页的深层节点，**不得用于交付或后续接入**；保留只为问题追溯。
- `*_V02.prefab`：从 `RND/StructurePrototype/ART006_UI_StructurePrototype.unity` 中的完整已验收节点树直接生成的独立候选。已绑定已验收的终端壳、退出键、HUD、总览、任务、对象、规则、统计和影响确认素材；仍须完成视觉状态、TMP 占位、焦点与多比例截图验收后才可交技术验收。

本目录不覆盖 `RND/StructurePrototype`；后者始终只承担机械布局合同验证。
