# ART-006 结构原型（非正式交付）

本目录仅保存 `FieldHudForm` 与 `BaseCommandHubForm` 的布局合同验证原型。

- 不含已验收的完整通用 UI 素材；不得作为正式视觉 Prefab 或进入主工程交付。
- 不含 GF UIForm 生命周期、C#、业务请求状态、资源加载键或客户端接入。
- 正式候选必须在通用素材（终端壳、退出键、页签状态、九宫格面板、滚动条、焦点与反馈状态）完成用户验收、Sprite 导入与 Importer 验证后，从已验收 `prefab-layout.md` 重新生成。
