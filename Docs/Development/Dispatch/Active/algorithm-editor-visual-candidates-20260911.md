# 算法编辑器效果图候选

2026-09-11用户明确批准制作人两项建议，其中2D进入算法编辑器效果图生产。任务 `art2d-algorithm-visual-candidates-20260911`。

## 输入与范围

### 2026-09-12用户确认最小返修及本地尺寸标准化

用户已明确确认制作人列出的最小返修方案，恢复同一任务，不重新设计整体外观。以v01最新四张推荐候选为输入，旧native与全部证据保留；新输出位于同一AlgorithmEditor目录下 `VisualCandidates/v02-semantic-correction/`。

1. 四图导航均明确为本机载体自身能力，不再显示“可选装导航组件A-01”，不列机械臂等为导航组件候选；保留已修正的任务分类。
2. 集中绑定示例改为确有候选的传感器输入节点，导航节点不承担选装组件绑定；草稿必填计数、底图节点状态与抽屉一致，仍由玩家明确选取，不自动绑定。
3. 失效示例区分草稿与旧版引用：例如草稿S-02失效、旧生效版仍使用有效S-01；不能同时声称同一必要硬件已被移除而旧版仍必然有效。
4. 历史页“等待安全点”改为明确非交互状态条，保留请求修订/更高草稿/生效版本分离，不暗示重复提交按钮。
5. 用户明确授权四张最终候选本地等比缩放、必要时补边到1920×1080；保留完整UI、不裁切、不非等比拉伸、不改变造型。此授权只限机械尺寸标准化，不涵盖脚本重绘整页、文字语义修补或拆图。记录原始尺寸、缩放系数/补边、源与输出哈希，注明上采样来源，不能宣称原生1920细节；语义改图仍用原生图像编辑工具。

以上覆盖旧尺寸处理未授权的等待，不再为同一事项请求放行。完成实际开图自检四状态语义及精确尺寸，发送制作人报告后用户作最终视觉决定；不因本次返修授权直接进入切图/Prefab/Unity。字体、Git、xlsx不操作。可将冻结的机械尺寸包直接交快速执行，但负责人保留最终图像复核。

基于ArtResource `Docs/ArtPipeline/UIRequirements/AlgorithmEditor/InteractionContract_20260911.md`、主仓库 `Docs/Development/Dispatch/Planning/Algorithm-Interaction-ProducerReview-20260911.md` 与B15已确认技术路线。既有准备和交互文档已完成，不重复盘点；字段保持建议/实现区分。DEC097影响文案明确：结构应用仅重置修改实例，同机其他算法在重启期间暂停且保留状态；参数安全应用不重置。

连续绘制1920×1080候选，覆盖常规编辑、集中绑定、问题定位与历史诊断四个代表状态。使用一致编辑器外观和同一合理图示例，示例只表达交互，不伪称实际运行/正式模板，不冻结五模板成本；保持Data/Event、草稿/生效/等待/红绿有效性、当时/当前明确区分，不新增断点/单步、自动绑定或手动作业按钮。

同一游戏工业科技风格；继承已验收基地中枢/机器硬件UI的配色材质语言，但根据算法编辑用途独立设计外观，不能机械套用象牙白基地壳，也不能只用简陋线框代替正式效果图。给画布和节点足够阅读空间，真实装饰不得挤占状态与操作。五区布局为合同草案，必要视觉调整记录不擅改业务职责。字体由用户后续统一，不开展字体工程或多比例适配。

## 输出与门禁

允许ArtResource `Docs/ArtPipeline/UIRequirements/AlgorithmEditor/` 下新增VisualCandidates/v01图像、提示词/来源清单与视觉检查报告；不覆盖旧文档或已通过机器/基地资源。用实际图像生成能力与适用技能，不以脚本画框代替效果图。无Unity、切图、Prefab、C#、Git或xlsx操作。

自检必须打开实际落盘图，对照功能/布局状态，报告满足项、问题、证据及影响，不以文件非空代替视觉核验。整批先实际发送制作人报告，并同步客户端为参考；不直接向用户宣布通过/请求跳过制作人。由制作人检查后用户作最终视觉决定，确认前不进入拆图/Prefab。

普通进度不中断，完成四状态及报告再complete --claim-next并实际回传；只在真实外部决定阻塞所有安全步骤时block让位。无法安全拆分的专业绘图不强行交快速执行。
