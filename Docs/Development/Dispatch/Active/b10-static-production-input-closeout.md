# B10 轴测通过后的生产输入收口

> 最新续接：本页文档修订已完成并经制作人复核，用户已确认5+7分支、尺寸后续调整及DEC-196。现按 [生产图与说明派发](b10-production-views-and-notes.md) 执行B10 3.4；该派发覆盖本页历史“尚不出图”的阶段限制，仅授权五项三视图与七项说明，不授权建模或Unity。

2026-09-10 用户明确通过四张返修图。此为art-3d现有Active的依赖解除/文档续接，不创建第二Active，不恢复B08。

## 参考版本

ArtResource `ArtSource/Concept/b10-interactive-world-and-static-art/Axonometric/`：
- v02-minimal-rework：BasicComputeCore、CommunicationTransmitter、ExplorationScanner、ManufacturingWorkshop四张v02。
- v01：ObjectStateSensor、SoilSensor正式非rejected、CommunicationReceiver、BasicWarehouse、BiomassGenerator、SolarGenerator、BasicBattery、ShoreWaterPump。

用户确认造型，不等同从图冻结新尺寸、孔距或程序配置。

## 主美当前可连续执行的工作

仅在现有B10 Preproduction授权目录同步README/Manifest/相关合同的最新图路径与状态，保留旧版本来源；完成生产输入收口记录。
说明核心薄鞍、两宿主发射器承接、扫描器固定支撑、工坊两输出位置与中央入口关系；继承已有合同，不把参考灰块当B08几何，不按图片像素造尺寸。
列出尚未冻结且真正阻止建模的差异及可用建议；能继承的静态紧固、仓库/能源/水泵通用规则直接引用，不反复询问。未实测接触/可达明确为后续模型自检。
逐项建议三视图或“轴测+尺寸说明”分支及理由，供制作人核对后用户筛选；此阶段不出图、不启动Blender/Unity/建模，不改主工程配置。
完成实际回传制作人：精确文档路径、参考映射、必要待决项、建议生产图分支。无需等待普通已阅。

## 2026-09-10 用户确认最小文档修订

制作人已实际阅读收口记录、ART013/ART015合同、配合补充、Manifest，并查看核心/扫描器/工坊v02与水泵v01。用户确认按以下范围继续；仅续接现有B10任务，不授权出图、Blender、Unity、建模或B08修改。

1. 保留已确认造型比例，重新提出核心、扫描器的可装配尺寸建议；同步本体尺寸、适配和总包络，不能为迁就旧P数值压扁造型。新值明确标为待复核建议，不按像素测量冒充实测。
2. 工坊现行正文/表格仅保留当前建议output_a/b=(±1.80,0.55,2.60)m、每台1.0×0.8m、中央空带2.60m；旧±0.65移至历史记录，避免页首覆盖正文造成误用。此为文档布局建议，不写配置、不宣称取货可达已验证。
3. 水泵分别列岸上主体高1.1m建议、入水段、完整X/Y/Z上下界；修正Manifest将主体高当完整包络的写法。管口Y=-0.25仍是建议，完整下界须包含管体，不仅管口中心。
4. 已由用户后续决定撤销标准安装岸段前置：暂不考虑地形，地形自适应由用户后续程序方案处理。当前美术不等待岸高、水位、岸形、吸附或地形安装补证，不要求额外岸线剖面；保留水泵自身结构连接、本地尺寸与完整包络、管口位姿及交接接口，不为地形修改已确认造型。既有岸段数值仅历史建议，不作为放置限制或当前待决项。
5. 用户同意生产图分支：SoilSensor、ExplorationScanner、CommunicationTransmitter、ManufacturingWorkshop、ShoreWaterPump五项补三视图；其余七项轴测加尺寸/必要安装说明。仅确认分支，技术文档再次复核及后续授权前不派发出图。

交付为同一Preproduction目录内最小修订、统一后的Manifest和差异记录。实际回传制作人全部路径、未决项、已继承规则和建议值。不得以文档修订撤销已通过原画或旧模型验收。无需为普通中间进度暂停；仅不可继续的真实依赖才等待。

### 用户后续尺寸处理决定（2026-09-10）

针对核心与扫描器新建议包络在轮式宿主横向余量为0的文档复核提示，用户明确“后续调整大小就行了”。取消将该项作为当前文档阻塞及单独分层截面/净空补证前置；新尺寸保留可调整建议性质。后续模型生产获授权后，在实际装配自检中调整新增组件本体大小及对应适配尺寸，优先保持已认可造型比例，不改B08载体/锚点，不新增组合限制；不能以此宣称当前安装已通过。若缩放影响适配，保持与宿主的接触关系，不简单缩放整个挂接层导致脱离。重大造型或功能改变仍报告用户。本决定不单独授权出图或建模，水泵场地依赖不扩散至其余资产。

### 客户端协作执行范围

状态：该只读协作已完成；用户随后取消岸段方案的美术前置，以下保留历史范围，不再次派发、不请求岸段裁决。后续地形自适应未实现，也不因取消美术依赖获得程序实施授权。5+7生产图分支仍有效，出图/建模门禁不由本决定自动解除。

队列任务 `b10-client-pump-installation-input`：在当前Active安全完成后，读取主美上述目录的水泵建议与现有场地/建造设计及代码，只读核验已有安装输入。仅可新增或更新 `openspec/changes/b10-interactive-world-and-static-art/evidence/client-pump-installation-input.md`，列已有依据、缺失项、推荐标准岸段的参数建议、主美需保留的接口及未来验证条件；不实现岸线/地形/建造代码，不修改配置、xlsx、B08、Unity或Git。不将设计中的通用规则冒充已有实现。完成后实际回传主美及制作人，主美据此汇总待决项；未冻结值不得直接写成正式门槛。
