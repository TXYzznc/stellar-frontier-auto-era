# B15 算法核心服务实施派发

## 用户确认与范围

2026-09-11用户对制作人推荐的“纯C#核心＋现有GF/UGUI；先核心服务，再UI，最后生产模板”明确回复“确认”。依据 `Docs/Development/Dispatch/Planning/Algorithm-Implementation-Proposal-20260911.md`，批准A路线及S1–S3第一批实施，S4交互设计并行、实际UI生产/接入另有门禁，S5真实生产与五模板仍后续收敛。

本条允许在现有B11–B14已交付基础上推进这一独立服务子集，不将任务表G2或完整P0-011判为已完成，不更改任务表依赖。五模板新节点/成本没有冻结，不为保留旧数字隐藏步骤。B10模型材质/正式交付不在本次确认内。

## client：b15-client-algorithm-core-services

从已完成提案继续，不重复准备任务。先按OpenSpec技能为 `b15-algorithm-core-services` 建立proposal/design/specs/tasks，依CLI模板与依赖顺序生成，列任务表真实ID及部分覆盖界限。严格校验通过后连续实施S1→S2→S3，不以artifacts完成为暂停条件，不再请求普通逐段放行。

### 本批交付

1. S1：图文档/稳定ID、固定节点与强类型端口目录、单位/能力/绑定/结构/容量验证、基础纯运算与不可变执行计划。采用已确认类型家族及受限结构，不自行创造语言规则；发现实质合同冲突仅挂起受影响部分，先做无冲突工作。
2. S2：触发快照、绑定代次、算力调度、批次状态和指令提交、任务/导航端点、结果与因果诊断、取消/世界退出。复用B11–B14权威服务，不从MotionGraph推导世界产出。实际闭环为基础传感状态→显式判断→机器任务→真实导航→结果诊断；测试图不成为正式开局模板。
3. S3：草稿/生效版本与运行态隔离、安全应用/版本重验、结构与参数变更差异、系统/玩家模板服务和清绑定、内存恢复DTO。不得声称磁盘存档、离线推进或五生产模板完成。
4. 将同一机器的硬件UI观察、导航、传感器公开快照联合生命周期纳入本批集成验证，复用已验收面板；不新增玩家手动作业入口、完整购买部署、能源或农业系统。纯夹具与真实服务证据分开，不以单元通过替代真实NavMesh/区域集成。
5. 持续向2D点对点提供字段/意图/状态和稳定性标识，不以未完成UI阻塞服务实现，不打断其Active去派发新任务。

### 允许与禁止

允许：本change全部artifacts/evidence；按需新增 `Assets/Game/Scripts/AutoEra/Algorithms/` 实际文件及.meta；本批 `Assets/Game/Tests/AutoEra/Editor/`、`PlayMode/` 算法与联合测试及.meta。适配可在 `Assets/Game/Scripts/AutoEra/Machines/`、`World/Region/`、`Application/` 内最小修改现有服务消费层/生命周期组合，不重写原权威服务；每个结构工作单元实施前在change证据列精确文件、职责与兼容影响，不新增空目录骨架。

允许协调8090进行普通编译及独立可恢复TestRunner测试，加载既有Launch/InitialRegion与正式机器资源；测试输出限Temp和原生测试报告，取证前保留仍需引用的旧XML/截图，测试后释放场景/监听/租约及8090。不得覆盖用户未保存场景。正式场景/Prefab/图/素材/配置文件本批不改；若实际接入确实需要这些写入，先列精确路径和差异请求授权，同时继续其它安全服务工作。

禁止：ScriptsBuiltin、产品asmdef/新依赖、手改生成代码、任何xlsx或任务表、字体、多比例、原模型/材质、现有Motion合同重做、Git操作。GameData如后续确需变更，先明确精确JSON与正式生成范围，不由此泛化数据授权。

### 验证与完成

按提案第8节落实模型/求值/调度/应用/恢复测试，以及真实B14→B11→B13集成；明确错误零提交、等待不变红、无自动重试/去重/自动绑定、版本及回调失效、关闭UI不取消请求。S1–S3实现覆盖与完整P3/G3区分，未就绪生产端点显式报告不可用，不能填假值。

普通编译、相关回归、Console、引用、OpenSpec strict、产品纯度/边界与diff检查均留实际证据。快速执行候选由客户端每个冻结工作单元直接检查/派发，QA可由客户端派精确差异验证包；不用制作人作普通测试中转，不为忙碌重复历史测试。

任务未完且下一步安全持续执行；需要决定仅block对应剩余单元让位。完成后实际回传制作人交付、测试、未覆盖、资源释放与下一队列，再complete --claim-next。新用户视觉仅由用户裁决，不能自行宣布完整G2/G3通过。
