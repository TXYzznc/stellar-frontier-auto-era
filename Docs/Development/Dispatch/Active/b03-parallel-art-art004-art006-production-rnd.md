# b03-parallel-art-art004-art006-production-rnd：美术生产路线预研

> 本文件是OpenSpec建立前的方案探索派发单，不代表生产方案已经确认，也不改变任务表状态。

> 自2026-08-24起采用高自治协作：主美与2D窗口自行记录两轮增量归档，直接询问用户并点对点
> 同步受影响窗口；Git提交统一交`AutoEra｜Git集成`。普通讨论、归档和进度不回传制作人。

## 1. 身份信息

| 字段 | 内容 |
|---|---|
| 派发ID | `b03-parallel-art-art004-art006-production-rnd` |
| 管理批次 | `b03` |
| 原始任务ID | `ART-004`、`ART-005`、`ART-006` |
| 专业窗口 | `AutoEra｜主美（3D）`主责；`AutoEra｜美术（2D）`负责ART-006专项输入 |
| 协作窗口 | `AutoEra｜程序（客户端）`评审动作与Unity接入契约 |
| 协作状态 | `ART-004/005 OpenSpec就绪；ART-006继续专项收敛` |
| 制作人升级入口 | `AutoEra｜制作人`；仅处理强制升级和批次完成 |
| Git提交入口 | `AutoEra｜Git集成` |

## 2. 目标与范围

### 目标

在已冻结视觉基线、场景混合路线和ART-001交付合同上，确定可重复量产的模型／材质／模块化
路线、机器程序化动作实现矩阵，以及HUD／管理界面视觉体系，再建立对应OpenSpec。

### 玩家或开发价值

让后续正式资源能够按统一预算和合同并行生产，机器动作可以由程序状态安全驱动，核心界面
在1920×1080下保持明亮、清晰且具有一致的工程科幻语言。

### 范围

- `ART-004`：低多边形、风格化PBR、手绘与混合路线对照；贴图、材质、LOD和模块化标准。
- `ART-005`：机器刚性关节程序动画的动作矩阵、参数、循环、中断、安全姿态和跨项目规则。
- `ART-006`：HUD、中枢、现场面板、节点编辑器候选、警报、规则弹窗和图标视觉层级。
- 三项生产路线之间的颜色、材质、功能分区、状态反馈和性能预算一致性。

### 明确排除

- 不批量制作第一版正式资源，不制作动物。
- 不实现Motion Core／Editor或AutoEra Adapter代码。
- 不确定最终节点编辑器是否进入第一阶段；只为可能存在的节点界面建立可扩展视觉语言。
- 不导入主工程正式资源，不修改任务表。

### 任务表完成定义

- `ART-004`：用同一对象完成候选路线对照，最终路线具备明确预算和可重复制作流程。
- `ART-005`：形成动作实现矩阵，每类核心动作都明确实现方式、输入参数、循环与中断规则。
- `ART-006`：关键界面具有同一视觉体系的高保真样张，信息密度适合1920×1080。

## 3. 已确认输入

### 正式来源

- 已归档`b02-parallel-art-art001-art003-visual-foundations`及其evidence。
- `Docs/GameDesign/90-设计管理/方案讨论增量归档.md`中的视觉、场景和程序化动作检查点。
- `Docs/Development/Dispatch/RoleRouting.md`
- `Docs/GameDesign/05-开发计划/第一版开发任务表.xlsx`（永久只读）
- `ArtResource/Docs/ArtPipeline/`中的ART-001～ART-003合同、基线和移交资料。

### 不重新开放的结论

- 整体风格为明亮、卡通化、工程科幻；中等面数、有可读细节，不追求写实高精度。
- 视觉基线以A“清朗拓荒工程”为主，吸收B的局部异星高彩与C的功能分区／安全标识。
- 场景采用Terrain＋模块化＋手工英雄点＋编辑器生成工具的混合路线，不做运行时随机地图。
- 当前不制作动物；主要动画来自机器。
- 机器主体使用可动部件、正确Pivot／轴向和程序化刚性关节，不以关键帧作为主体动作方案。
- 玩法逻辑是唯一权威，动作只读取状态／阶段／目标／进度并负责表现。
- 首阶段动作纵向切片是一台轮式载体、一个多关节机械臂和一个可替换效应器。

### 可复用实现、资源和工具

- ART-001合同样例、固定GUID交付包与动作关节JSON。
- ART-002融合视觉基线场景和ART-003混合场景样片／性能证据。
- ArtResource Unity 2022.3.62f3、URP 14.0.12、Unity Skills端口8091。
- 图像生成／编辑能力、Unity LookDev与后续Blender制作能力。

## 4. 专业窗口启动检查

- [ ] 主美与2D窗口分别阅读派发单、ART-001～003移交和自身优先SKILL。
- [ ] 主美检查代表性3D对象、材质、模块、LOD和动作关节合同的可复用状态。
- [ ] 2D窗口检查现有UI结构、玩家可见信息规格和1920×1080关键界面范围。
- [ ] 未取得美术工程Unity／DCC锁前不驱动8091、不修改场景或源文件。
- [ ] 向用户解释候选路线、比较维度、产出成本和后续量产影响，再开始试制。

## 5. 待讨论问题

### ART-004模型与材质路线

- 代表性对照对象选什么，如何同时暴露硬表面、模块拼接、状态色和俯视可读问题。
- 风格化PBR、手绘和混合方案的纹理通道、分辨率、材质复用、顶点色／贴花职责。
- 中等面数的近／中／远预算、LOD层级、阴影分级、合批／实例化与模块粒度。
- Blender到ArtResource再到主工程的命名、导出、校验和返修闭环。

### ART-005程序化动作矩阵

- 基础运动原语与机器动作清单如何映射，哪些姿态需要资源侧明确制作和验证。
- 参数、局部轴、范围、速度／加速度、循环段、中断策略、安全收回和断电恢复的资源合同。
- 主美样机、Motion Core和AutoEra Adapter之间的责任边界与跨项目预览方式。
- 哪些表现可以使用程序噪声／曲线，哪些例外才需要短关键帧或VFX辅助。

### ART-006 UI视觉方向

- HUD、中枢、现场面板、警报、规则弹窗和图标的共同组件语言与信息层级。
- 1920×1080基准下的安全区、字体、字号、密度、状态色与色弱冗余线索。
- 明亮场景中UI的对比、透明层、描边／阴影、工程图形符号和异星高彩点缀比例。
- 节点编辑器需求待定时，高保真样张如何覆盖可扩展节点／连线语言而不锁死功能结构。

### 联合评审

- 三项候选采用哪些统一截图、动图、矩阵和性能数据进行比较。
- 哪些结论进入Art Bible／动作合同／UI kit，哪些预算等待代表性正式资产Profile后确定。

每个窗口与用户每累计两轮讨论至少自行增量归档一次。主美负责汇总跨领域决策，2D窗口负责
ART-006专项结论；双方直接相互同步，不请求制作人代写。完整决策摘要取得用户确认后，才可
创建OpenSpec artifacts。

## 6. 修改权限与占用

### 可写范围

- 主美窗口获准写入主仓库精确目录
  `openspec/changes/b03-parallel-art-art004-art005-production-rnd/`，用于维护任务勾选、实施证据和
  经用户重新确认后的方案修订；不得借此扩大到其他主仓库路径。
- ART-004/005 的源资源实施仍只发生在 `ArtResource`；Unity 8091、Blender、场景、`.blend`、
  共享材质和资源目录锁由主美与2D窗口按实际文件点对点协调。
- ART-004任务2.1放行主美窗口对`ArtResource/ArtSource/ART004_ModularWorkshop/`和
  `ArtResource/Assets/Art/Authoring/ART004_ModularWorkshop/`的排他写入；只允许该任务的
  Blender源与Unity可用源，不得扩展到共享材质、其他场景或ART-006目录。
- ART-006 继续由2D窗口维护专项方案；不写入上述 ART-004/005 OpenSpec。
- 主仓库 Git 暂存与提交仍只交 `AutoEra｜Git集成`窗口。

### 高冲突单文件锁

- `ArtResource/ArtSource/ART004_ModularWorkshop/`：主美（3D）持有任务2.1排他目录锁，完成
  2.1验收或明确释放时结束。
- `ArtResource/Assets/Art/Authoring/ART004_ModularWorkshop/`：主美（3D）持有任务2.1排他
  目录锁，完成2.1验收或明确释放时结束。
- 共享材质、其他场景、`.blend`和ART-006源文件仍未授权，使用前另行点对点登记。

### 只读范围

- `Docs/GameDesign/05-开发计划/第一版开发任务表.xlsx`
- 除上述精确 ART-004/005 change 目录以外的主仓库内容、已归档OpenSpec、ART-001～003移交资料
  和主工程产品代码。

### 禁止修改

- 主工程`Assets/Game/ScriptsBuiltin/`
- 任务表、ART-001固定GUID交付合同和已归档证据。

### 工具独占

| 工具 | 实例／端口／文件 | 当前占用者 | 释放条件 |
|---|---|---|---|
| 美术工程Unity | `ArtResource`／8091 | 条件放行主美任务2.1；当前未占用 | 主美取得2D窗口确认并核验目标项目后独占一段时段，完成操作立即释放 |
| Blender／外部绘图工程 | `ArtSource/ART004_ModularWorkshop/` | 主美任务2.1目录锁 | 每个源文件仍保持单一窗口持有，任务2.1验收或明确释放时结束 |

### Git集成

- `ArtResource`当前不是Git仓库；正式量产前另行讨论独立版本库或交付检查点策略。
- 主仓库方案文档统一交Git集成窗口精确提交，只提交不推送，禁止包含任务表和未授权xlsx。

## 7. 交付与回传要求

- ART-004候选路线、统一对象、比较矩阵、预算和推荐量产流程。
- ART-005动作实现矩阵、资源合同补充、样机范围和程序协作需求。
- ART-006关键界面清单、候选视觉语言、高保真样张计划和可读性验收方式。
- 用户已确认／未确认决策、OpenSpec边界建议、试制写锁与工具锁需求。
- 可视验收入口、截图／动图／Profile证据计划、已知风险和依赖变化。
- 任务表状态／工时手动更新建议；不得修改任务表。

## 8. 暂停与升级条件

- 需要推翻已冻结视觉基线、混合场景路线或程序化动作主路线。
- 试制要求修改ART-001固定GUID／目录／锚点合同或主工程公共接口。
- 主美与2D窗口争用ArtResource Unity、共享场景、材质或DCC源文件。
- ART-004～006无法形成共同生产边界，需要调整OpenSpec分组。
- 发现任务表依赖／DoD需要改变。

发生以上情况且点对点协调仍无法解决时，停止相关工作并升级制作人，不自行扩大授权。

## 9. 增量讨论检查点

### 2026-08-24：ART-004／ART-005第一组生产路线决策

以下内容已经用户确认，可作为后续讨论的冻结输入；尚未完成的决策继续保持开放，不据此创建
OpenSpec或开始资源试制。

- `ART-004`代表对象采用独立模块化工坊／处理站，用于同时验证硬表面、模块拼接、状态色和
  俯视可读性。
- 模型与材质采用混合路线；LOD采用按资产等级划分的平衡分级，不要求所有资产使用同一固定
  层数或预算。
- `ART-005`代表对象采用独立轮式载体、多关节机械臂和可替换效应器的组合。
- 动作语言采用清晰分段的工程化表现；中断／异常时进入受控安全收回，不采用瞬间复位。
- 第一阶段动作验证覆盖6个核心运动原语；玩法状态仍是唯一权威，动作系统只负责表现。
- 作业进度映射采用方案B：程序只提供稳定的作业阶段、`0～1`进度、效率档位和中断状态；
  资源表现据此调节作业循环速度、停顿、状态灯和临近完成收束，不依赖复杂玩法细节。
- 当前开放问题：后续`ART-005`剩余动作合同细节与`ART-006`视觉语言决策。

本检查点由主美窗口主动跨窗口回传，未占用ArtResource 8091、Blender、文件写锁或Git索引。

### 2026-08-24：模块尺度、远距反馈与UI表面语言

以下跨`ART-004`～`ART-006`结论已经用户确认，作为后续工坊模块、机器状态表现和UI样张的
共同输入：

- `ART-004`工坊采用模块化中等标准跨距：以`1 m`为基础网格和最小单位，规范模块尺寸采用
  `2 m`、`4 m`、`8 m`，用于保持资产复用、拼装灵活性和俯视尺度可读性。
- 远距离状态反馈采用三层方案：机器动作、世界内状态灯／色带、HUD简要提示共同表达状态，
  但不重复展示完整信息。
- 状态语义暂定为绿色表示正常作业，黄色表示对位／收回／等待，红色表示故障或中断；必须同时
  使用图标、形状或闪烁节奏提供色弱冗余，颜色不得成为唯一线索。
- `ART-006`主表面采用“清朗工程面板”：暖灰白实色卡片、深蓝灰结构线、受控橙／青绿强调，
  不使用深色工业底盘或大面积玻璃化作为主要界面语言。
- 信息架构采用“极简HUD＋中枢”：HUD只保留即时摘要、高优先级告警、当前对象和入口；生产链、
  库存、队列、统计、规则与历史从HUD按钮或中枢按需展开。

当前仍需继续讨论字体／字号、关键页面布局、状态冗余细则和其余资产预算。本检查点不授权试制，
不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：首套工坊模块库与表面复用职责

以下`ART-004`结论已经用户确认，用于约束第一套工坊／处理站候选资产的制作范围和表面方案：

- 首套模块库采用“骨架＋功能接口”结构。骨架模块包含地板、墙、柱、屋顶和转角；功能接口包含
  门、入口平台、管线接口、操作面板、状态灯及少量围栏。
- 丰富装饰和室内细节不提前纳入首套模块库，避免在基础拼装、尺度和接口规则尚未验证前扩大范围。
- 贴图以两类主职责Trim Sheet为基线：结构件Trim Sheet、功能件Trim Sheet，并配合基础平铺
  材质完成大面积表面。
- 画面质量不得因复用策略而过于简陋；当复用价值和视觉收益明确时，允许进一步细分Trim Sheet，
  并保留局部贴花或专属遮罩表现英雄细节。

当前仍需通过统一代表对象验证模块丰富度、材质层次和俯视可读性。本检查点不授权试制，不创建
OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：建筑细节层级与程序化动作编排

以下`ART-004`／`ART-005`结论已经用户确认，用于约束代表性工坊资产的视觉密度和机器动作
实现边界：

- 建筑细节采用分层集中：入口、操作面板、功能接口、状态灯和正立面承担最高细节密度；中层
  保留清晰模块节奏；远距与大面积表面保持干净轮廓和色块，避免平均铺满细节。
- 所有机器主体动作保持程序化，不使用关键帧驱动姿态。VFX可以由程序化动作的阶段事件触发，
  但不得接管关节姿态或成为动作权威。
- 动作复杂度通过分阶段曲线与关节编排实现：每个运动原语可配置各关节曲线、时序、停顿和事件
  标记，并保持既定的安全中断规则与参数化复用能力。
- 第一版不采用完全物理仿真驱动机器主体动作；物理反馈不能破坏玩法状态权威、确定性阶段和安全
  收回合同。

当前仍需继续收敛曲线数据格式、事件标记合同、VFX触发边界和代表性原语样张。本检查点不授权
试制，不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：字体体系与详细界面双层展开

以下`ART-006`结论已经用户确认，补全“清朗工程面板＋极简HUD＋中枢”的文字与展开层级：

- 字体采用中性工程无衬线体系。标题、正文和导航保持清晰克制；数字使用等宽且略具工程感的
  字形，便于资源量、时间、效率和状态值对齐比较。
- 详细界面采用双层展开：HUD入口先打开右侧轻量面板，承担当前告警、对象摘要和快捷操作；
  生产链、规则、统计等复杂管理进入中枢全页。
- 右侧轻量面板不得演变为第二个完整中枢；HUD继续只保留即时摘要、高优先级告警、当前对象和
  入口，复杂历史与跨系统管理不常驻屏幕。

当前仍需继续确认字号阶梯、最小可读尺寸、数字字形候选和关键页面布局样张。本检查点不授权
实施，不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：关节参数模板与工程使用痕迹

以下`ART-004`／`ART-005`结论已经用户确认，用于约束机器资产配置复用和工坊表面痕迹分布：

- `ART-005`关节配置按类型采用可复用参数模板。轮组、旋转、伸缩和效应器接口分别提供默认
  速度、加速度、回弹、限制与安全规则；具体资产只覆盖与模板不同的参数，避免逐资产重复配置。
- `ART-004`采用克制的工程使用痕迹。磨损、积尘、热痕和维修标记集中于入口踏面、平台边缘、
  功能接口、散热口、维护面板和作业端，大面积表面继续保持清朗。
- 痕迹依据功能位置和实际使用逻辑分布；同类模块共享规则，同时允许实例级轻微变化。禁止均匀
  随机脏污，也不要求每栋建筑完全独立手绘。

当前仍需继续收敛模板数据结构、覆盖层级、痕迹强度范围和代表性样张。本检查点不授权试制，
不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：对象优先轻量面板与现场告警聚焦

以下`ART-006`结论已经用户确认，用于约束右侧轻量面板的信息顺序和告警点击后的交互路径：

- 右侧轻量面板采用“对象优先”。选中对象时固定顺序为名称／状态、当前任务与阻塞原因、
  `2～3`个快捷操作、中枢详情入口；未选中对象时回退为告警和最近任务摘要。
- 点击告警时优先聚焦对应现场对象，再由右侧面板展示原因、影响和可执行操作；只有需要深入
  分析时才进入中枢详情，不让普通告警直接打断现场观察并跳转全页。
- 该交互继续遵守极简HUD、现场优先和按需展开基线；右侧面板不承载完整历史、跨系统统计或
  复杂规则编辑。

当前仍需继续确认告警无对应对象时的降级路径、快捷操作安全边界和面板布局样张。本检查点
不授权实施，不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：任务导向中枢导航与待处理优先总览

以下`ART-006`结论已经用户确认，用于约束中枢全页的一级导航和总览页信息层级：

- 中枢采用任务导向主导航，固定顺序为：总览、待处理／任务、对象与系统、规则／自动化、统计。
  导航按玩家要完成的管理行为组织，不按程序模块或数据类型机械拆分。
- 中枢“总览”页采用待处理事项优先：严重告警与阻塞置顶，当前任务／队列居中，资源／产能摘要
  置底。摘要只提供决策入口，不在总览堆叠完整报表。
- 该层级与既定信息链一致：HUD告警先聚焦现场对象，对象优先右侧面板提供原因与快捷操作，
  需要深入管理时再进入对应中枢详情。

当前仍需继续确认各导航项的二级页面、总览卡片数量上限和无待处理事项时的空状态。本检查点
不授权实施，不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：规则卡片、影响预览与程序化表现合同

以下`ART-005`／`ART-006`结论已经用户确认，用于约束首阶段规则编辑体验、轮式载体贴地方案
和可动资产跨类型接入边界：

- `ART-006`规则／自动化首阶段采用规则卡片与表单优先，以“触发条件→判定→执行动作”的
  可读卡片、列表和弹窗为主；节点／连线仅作为后续扩展语言，不构成首阶段前置功能。
- 规则保存或启用前必须提供影响预览，清楚展示触发时机、影响对象和执行动作，并明确区分
  “仅保存草稿”与“立即启用”。
- `ART-005`首个轮式载体采用四轮独立地面接触采样、悬挂贴地和四点平滑车体姿态拟合；轮子
  半径用于台阶高度阈值，连续坡度使用独立可配置规则。
- 可动资产统一采用六层程序化表现合同：可动结构、基础运动原语、环境感知、状态阶段、表现
  编排、细节反馈。美术定义结构、限制和表现参数，程序负责状态、求解与运行时控制。
- 合同面向移动机器、建筑机构和资源点复用；未来敌人只保留扩展契约，不纳入当前第一版制作。

当前仍需继续确认规则卡片字段密度、预览弹窗布局、坡度参数边界和六层合同的数据接口。本检查点
不授权实施，不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：纵向样机范围、图标语言与三级状态

以下`ART-005`／`ART-006`结论已经用户确认，用于限定统一程序化表现合同的首轮验证对象，
并统一界面图标和状态反馈层级：

- `ART-005`纵向样机由一台轮式机器和两个轻量建筑机构组成，仅用于验证统一程序化表现合同，
  不作为正式量产资源。
- 两个建筑机构采用滑动门和传送带。滑动门验证线性开闭、占用／告警和中断；传送带验证持续
  循环、进度／效率驱动和货物节奏；轮式机器的机械臂继续覆盖旋转与伸缩。
- `ART-006`采用线性工程图标与状态填充：默认图标保持克制，仅在激活、告警、资源焦点和关键
  操作时加入填充与工程强调色；关键图标仍需文字标签或说明补足。
- 状态采用严重、需处理、正常／未激活三级。HUD仅常驻严重项，需处理项进入队列，普通状态留在
  对象面板；颜色、形状／图标和文字必须共同表达，颜色不得成为唯一线索。

当前仍需继续确认纵向样机验收动作、货物占位表现、图标笔画规范和三级状态的具体视觉样张。
本检查点不授权实施，不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：异常趋势统计页与1080p HUD四角安全区

以下`ART-006`结论已经用户确认，用于约束统计页首屏的信息优先级和`1920×1080`现场HUD布局：

- 统计页采用异常与趋势优先。首屏展示偏离目标的指标、短周期趋势和原因入口；完整明细、长周期
  对比和全量报表按需展开，不在首屏平均铺陈所有数据。
- `1920×1080` HUD采用四角分区：左上显示资源摘要，右上显示时间／速度与中枢入口，左下显示
  严重告警，右下显示当前选中对象及快捷操作；中央区域保留给现场观察和对象交互。
- 四角信息继续遵守极简HUD和状态分层：需处理项进入队列，普通状态留在对象面板；右下对象区
  与对象优先轻量面板保持一致，复杂统计仍进入任务导向中枢。

当前仍需继续确认安全边距、缩放策略、超宽屏／低分辨率适配和统计图表样张。本检查点不授权
实施，不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：轮式机器坡度门槛与作业稳定支撑

以下`ART-005`结论已经用户确认，用于约束首个轮式机器的地形适应边界和作业展开／收回顺序：

- 首个轮式机器的连续坡度上限为`35°`；台阶高度以轮子半径为阈值。坡度与台阶独立判定，后续
  允许按机器类型分别配置。
- 作业稳定采用锁轮与可收放稳定支撑：完成对位后先锁轮，再放下两侧或后方支撑，随后展开
  机械臂；收回时严格反向执行，先收机械臂、再收支撑、最后解除锁轮。
- 可通行性由逻辑层裁决；表现层只能在允许范围内执行接触采样、悬挂贴地和车体姿态拟合，
  不得通过强行贴地跨越已判定不可通行的台阶或坡面。

当前仍需继续确认稳定支撑数量／位置、地面法线过滤和失败／中断时的安全收回表现。本检查点
不授权实施，不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：节点预留语言、UI遮挡与接触／IK边界

以下`ART-005`／`ART-006`结论已经用户确认，用于冻结未来节点编辑器的视觉语言、界面遮挡层级，
以及纵向样机的稳定支撑接触和机械臂目标合同：

- `ART-006`节点编辑器采用左进右出的预留语言：条件在左、判定居中、动作在右，使用稳定端口和
  单向连线；节点继续采用暖灰表面与深蓝灰结构线。该语言只用于未来扩展，不改变首阶段卡片／
  表单优先决策。
- 常规HUD与侧栏采用约九成不透明的半实色面板；只有规则确认、严重告警等模态弹窗轻度压暗
  现场背景。不采用全局毛玻璃，也不使用全实色面板大面积遮挡现场。
- `ART-005`可收放稳定支撑采用独立地面探测：各支撑在最大行程内抵达接触点，脚垫有限贴合
  地面法线，车体只做平滑补偿，不启用完整受力仿真。
- 机械臂作业目标提供目标位置、表面法线和推荐接近方向；IK在关节限制内使效应器合理对准
  采掘、装卸或维修目标，首阶段不实现完整碰撞求解或力反馈。

当前仍需继续确认节点尺寸／端口规范、模态压暗强度、支撑行程失败处理和IK不可达降级表现。
本检查点不授权实施，不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：可动资产净空锚点与重新对位

以下`ART-005`结论已经用户确认，用于约束可动资产的表现净空、稳定作业锚点和机械臂不可达时
的安全重定位流程：

- 可动资产提供`KeepOut`净空区域，以及`WorkPoint_*`／`Socket_*`稳定锚点，用于作业对位、
  效应器连接和避免明显视觉穿插；逻辑碰撞、导航与最终可通行性权威仍由主工程拥有。
- 机械臂目标超出可达范围或净空不满足时，状态标记为“需要重新对位”。机械臂保持或回到安全
  姿态，底盘移动到合适作业位置后重新执行对位流程。
- 不允许机械臂强行伸到关节极限，也不允许IK、表现层或资产锚点忽略净空和逻辑约束继续作业。

当前仍需继续确认`KeepOut`形状表达、锚点命名校验、候选重定位点来源和重新对位失败反馈。
本检查点不授权实施，不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：参数化门族与传送带表现

以下`ART-005`建筑侧结论已经用户确认，用于约束滑动门和传送带纵向样机的复用方式及表现边界：

- 滑动门采用参数化门族，支持单扇／双扇、`2 m`／`4 m`／`8 m`模块宽度，以及可配置行程、
  速度、停靠点、状态灯和安全感应区；具体建筑只选择参数、外壳和细节，不重复制作独立逻辑。
- 传送带采用参数化表现：皮带／链板循环、滚轮转动、`LoadPoint`／`UnloadPoint`锚点，以及速度
  与效率参数共同驱动作业节奏。
- 传送带不采用只有滚动贴图的最低表现，也不实现完整物理货物模拟；货物节奏由稳定锚点、阶段
  和参数化表现共同呈现，逻辑状态仍由主工程拥有。

当前仍需继续确认门族参数默认值、安全感应区反馈、传送带货物可视数量和效率映射曲线。
本检查点不授权实施，不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：ART-006强制采用GF UI全流程规范

`ART-006`后续方案、OpenSpec、样张、UI美术交付、Prefab／代码接入和验收必须以
`Docs/Development/GF-UI-Standards/README.md`及`01`～`08`为强制工作流输入，后续派发单和
change必须明确引用，不得只依赖本派发单内的视觉摘要。

- 每个独立页面先定义目的、入口／出口、数据、操作、异常和全部适用状态，再形成状态表、
  `prefab-layout.md`、视觉稿和验收证据；`prefab-layout.md`确认前不得创建正式Prefab。
- UI美术交付必须包含设计token、唯一字体／TMP Font Asset、至少五级字号、可编辑源文件、切片／
  导入／九宫格说明、资源清单、视觉稿和验收截图。
- 每个独立页面对应一个GF UIForm Prefab；输入、生命周期、异步回调和资源注册遵守GF边界，
  不以运行时动态拼装正式页面层级或直接读取设备输入替代规范接入。
- `1920×1080`继续作为主视觉评审基准，但不是唯一适配验收。正式验收至少覆盖`16:9`、`16:10`、
  `21:9`，并包含鼠标、键盘、手柄焦点，可访问性、性能和`07-测试与验收清单.md`证据。
- 后续创建ART-006 OpenSpec时，必须把项目接入模板、页面状态表、layout／视觉稿、资源交付、
  Prefab／代码接入和多比例验收拆成可追踪任务与DoD。

本工作流更新不授权当前窗口实施，不创建Prefab或代码，也不占用ArtResource 8091、DCC、文件
写锁或Git索引。

### 2026-08-24：建筑安全反馈、轮距驱动与三张代表样张

以下`ART-005`／`ART-006`结论已经用户确认，用于冻结建筑机构异常反馈、轮式表现驱动，以及
后续高保真UI样张的最小覆盖范围：

- 滑动门遇临时占用时减速、暂停，并回弹或重新打开，使用黄色提示；只有持续异常或故障才转为
  红色，避免把可恢复占用误报为严重故障。
- 传送带下游堵塞时进入黄色等待并受控减速，链板、滚轮和货物保留当前位置；条件恢复后平滑
  继续，不瞬移货物或重置循环相位。
- 轮子转动由实际移动距离和接地状态驱动：真实移动才转动，停止即停；只有逻辑层明确给出打滑
  状态时才允许接地空转。
- `ART-006`后续高保真样张固定为三张代表界面：现场样张覆盖HUD、对象优先面板和严重告警；
  中枢样张覆盖任务导向总览；规则样张覆盖规则卡片和影响预览确认。
- 严重告警采用左下单条行动告警卡，只展开当前最高优先级的图标、短原因、影响对象和主操作；
  其他严重／待处理项归入计数队列，避免多卡同时遮挡现场。
- 三张样张以`1920×1080`为主设计评审基准，同时必须按GF UI规范验证`16:9`、`16:10`和`21:9`，
  不得把1080p截图当作唯一适配证据。

当前仍需继续确认门占用回弹参数、传送带恢复曲线、打滑状态合同、三张样张的状态表和告警队列
展开方式。本检查点不授权实施，不创建OpenSpec，也不占用ArtResource 8091、DCC或文件写锁。

### 2026-08-24：需处理告警队列与异步原位反馈

以下`ART-006`结论已经用户确认，用于约束“需处理”级告警的展开方式和异步操作的状态收口：

- “需处理”级告警采用折叠计数队列，不常驻展开挤占现场；用户点击后按影响程度和发生时间展开
  轻量列表，并可继续聚焦对象或进入相应详情。
- 异步操作采用控件原位反馈：操作按钮进入加载态并防止重复点击；成功后在触发对象、按钮或规则
  卡片原位短暂显示结果；失败时原位说明原因和可恢复动作。
- 全局Toast和频繁模态弹窗不作为常规异步反馈主路径；只有跨页面、系统级或必须阻断决策的事件
  才升级到全局提示或模态层。
- 后续页面状态表必须覆盖`loading`、`success`、`error`、`cancel`、超时、页面关闭和晚到回调，
  并遵守GF UI Standards的异步收口与焦点恢复要求。

当前仍需继续确认队列最大可见项、同对象告警合并、成功反馈持续时间和失败重试入口。本检查点
不授权实施，不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：键盘／手柄焦点与逐层关闭

以下`ART-006`结论已经用户确认，用于约束键盘／手柄导航的视觉焦点和Esc／取消输入层级：

- 键盘／手柄焦点采用橙色外框与轻微缩放共同表达；鼠标hover只作补充，核心操作和语义理解
  不得依赖hover状态。
- Esc／取消只关闭当前最上层可关闭界面，并在关闭后恢复触发该层的控件焦点；不得越级关闭
  底层页面，也不得从UI层级直接退出游戏。
- 规则确认弹窗、右侧轻量面板、中枢全页按此顺序逐层返回；顶层界面消费取消输入后，底层现场
  或页面不得重复响应同一次输入。
- 后续状态表、`prefab-layout.md`、Prefab和验收证据必须记录默认焦点、焦点顺序、关闭目标、
  焦点恢复目标，以及鼠标／键盘／手柄核心路径。

当前仍需继续确认焦点缩放幅度、不可用控件跳过规则、动态列表焦点恢复和系统级不可关闭错误。
本检查点不授权实施，不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：四轮转向、反向转向与蟹行

以下`ART-005`结论已经用户确认，用于约束首个轮式机器的底盘资产结构和低速机动表现：

- 首个轮式机器采用四轮转向；资产必须为每个车轮保留独立转向轴、悬挂行程、地面接触点和
  转向限制，以兼容既定的四轮独立接地和车体姿态拟合。
- 启用前后轮反向转向以缩小转弯半径；启用蟹行，使四轮同向转动，在保持车头朝向的情况下进行
  斜向或横向低速微调。
- 第一版不纳入高速同向转向；蟹行与反向转向属于受控低速机动，仍须服从`35°`坡度上限、
  轮半径台阶阈值、逻辑层通行裁决和作业重新对位流程。

当前仍需继续确认各模式转角上限、模式切换速度门槛、轮速差分和蟹行时的悬挂／车体表现。
本检查点不授权实施，不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：低速蟹行微调与效应器装卸序列

以下`ART-005`结论已经用户确认，用于约束机械臂展开状态下的底盘移动边界和可替换效应器表现：

- 大幅移动前必须完成安全收臂；只有低速蟹行微调可以保持抬起的对位姿态，此时效应器必须离开
  作业目标并保持在`KeepOut`净空内，机械臂只允许有限姿态补偿。
- 低速微调仍由逻辑层裁决可通行性和目标重定位；一旦净空、关节限制或稳定条件不满足，立即
  停止微调并进入安全姿态／重新对位，不允许边移动边强行维持目标接触。
- 可替换效应器采用参数化装卸序列：对位接口、锁扣解锁、旧效应器脱离、新效应器接入、锁定、
  状态确认。序列由程序化关节、线性／旋转原语和阶段事件触发完成。
- 第一版不采用即时替换，也不实现自由装配模拟；装卸必须经过稳定锚点、明确阶段和可中断的
  安全收口。

当前仍需继续确认蟹行微调速度／距离上限、机械臂补偿范围、锁扣状态反馈和装卸中断恢复点。
本检查点不授权实施，不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：空／禁用状态解释与减弱动态效果

以下`ART-006`结论已经用户确认，用于约束页面空状态、禁用状态和功能性动效边界：

- 空状态和禁用状态必须明确说明为空／不可用原因，并提供下一步入口、恢复条件或等待状态；
  禁止只做灰色禁用，也禁止用空白列表代替状态设计。
- 动效只保留焦点变化、面板展开、加载和成功反馈等功能性短动效；告警不持续闪烁，严重状态
  仍须通过图标、形状、文字和受控强调表达。
- 提供项目级“减弱动态效果”入口，用于关闭或弱化非必要动画；核心状态反馈和操作结果不能因
  减弱动效而消失。
- 后续页面状态表、视觉稿、`prefab-layout.md`和验收证据必须覆盖`empty`、`disabled`及减弱动态
  模式，并遵守GF UI Standards的可访问性要求。

当前仍需继续确认空状态文案模板、恢复入口位置、功能性动效时长和减弱动态的具体保留清单。
本检查点不授权实施，不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：装卸安全中断与统一工程动作语言

以下`ART-005`结论已经用户确认，用于约束效应器装卸异常收口和全部程序化机构的基础运动节奏：

- 效应器装卸在取消、断电或故障时采用可恢复安全中断，只在预先定义的安全阶段边界停止。
- 效应器尚未脱离时回退到原锁定状态；已经脱离时保持安全挂载或待处理姿态，不执行即时替换、
  强行吸附或无状态回滚，并通过黄色／红色状态说明等待或故障原因。
- 全部程序化机构采用统一工程动作语言：“预动作—加速—受控减速—轻微落定”。不同机构可配置
  时长、曲线、停顿和落定幅度，但不得破坏统一阶段语义。
- 第一版不采用纯线性匀速动作，也不加入大量随机弹性；必要的回弹、惯性和细节反馈必须受参数、
  状态和安全边界控制。

当前仍需继续确认装卸安全阶段表、断电保持规则、动作曲线默认模板和轻微落定幅度预算。
本检查点不授权实施，不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：ART-006语义Token与共享UI Kit边界

以下`ART-006`结论已经用户确认，后续视觉稿、UI Kit、Prefab、代码接入与验收必须遵守：

- 采用全局语义Token，至少统一定义`Surface`、`Text`、`Action`、`Focus`、`Danger`、
  `Warning`、`Success`、`Disabled`等语义名；具体色值与样式由Token集中承载。
- 视觉稿、UI Kit、Prefab和实现代码必须引用语义Token，不得在页面、组件或脚本中散落重复色值。
- 采用小型共享UI Kit：只有跨两个以上页面使用且职责稳定的面板、按钮、图标、状态标记和告警卡
  才进入共享层。
- 页面特有的信息结构、布局组合和交互流程保留在所属`UIForm`，不得为追求复用提前抽象万能组件。
- 后续方案、OpenSpec与交付物须同时引用`Docs/Development/GF-UI-Standards/`中的Token、Prefab
  与验收约束。

当前仍需继续确认Token命名表、主题值、组件变体矩阵及共享组件准入清单。
本检查点只冻结方案输入，不授权UI制作、Unity操作、Prefab／代码修改或OpenSpec创建。

### 2026-08-24：ART-004／005样机证据与暂定预算门

以下`ART-004`／`ART-005`结论已经用户确认，用于约束纵向样机的可验收输出：

- 样机证据采用固定机位截图、短动图／视频和基础性能读数三类材料共同构成，不以文字说明代替
  直观结果。
- 视觉与动作覆盖近／中／远可读性，平地／坡地／台阶，蟹行／重新对位，稳定支撑、机械臂、
  效应器装卸及安全中断，滑动门／传送带状态，以及机器动作、世界内状态灯／色带、HUD摘要
  三层状态反馈。
- 性能读数采用可调整的“暂定预算门”：针对代表对象和代表区域设置上限与观察线，超出时须
  记录原因并触发优化／复核，不能只采集数字而不形成门禁。
- 当前样机不冻结最终量产预算；正式量产前必须使用真实资产密度、代表场景和目标平台重新复核
  并确认正式预算。

当前仍需继续确认固定机位清单、动图时长、性能指标种类、代表对象／区域规模及暂定数值。
本检查点不授权实施，不创建OpenSpec，也不占用ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：ART-006面板、图标尺寸与危险操作确认

以下`ART-006`结论已经用户确认，用于约束后续共享UI Kit、美术源文件和确认交互：

- 面板资源采用一套九宫格基础面板，通过常规信息、强调操作、模态确认三种层级变体组织；
  仅使用边框、阴影、强调色和遮罩区分层级，不为每个页面单独绘制面板家族。
- 图标逻辑显示尺寸采用`20 px`、`28 px`、`36 px`三级：`20 px`用于列表辅助和次级状态，
  `28 px`用于默认操作、资源与对象状态，`36 px`用于严重告警、关键对象焦点和主操作；关键
  图标不得低于`20 px`。对应可编辑美术源分辨率固定为`32 px`、`64 px`、`128 px`，并在资源
  清单中记录用途、状态、透明边距和导入设置。
- 危险操作不得点击即执行。涉及删除、覆盖、稀缺资源消耗、放弃任务、清空队列或其他难以撤销
  的操作，必须先显示明确后果、取消路径和确认动作的模态确认弹窗；普通暂停、关闭和可逆调整
  不得滥用危险样式。

当前仍需继续确认面板九宫格边界、图标笔画与状态变体、危险操作的具体判定表。
本检查点只冻结方案输入，不授权UI制作、Unity操作、Prefab／代码修改或OpenSpec创建；未占用
ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：ART-006危险操作分级确认与长按反馈

以下`ART-006`结论已经用户确认，用于补充危险操作的交互、文案与客户端配置契约：

- 危险操作按实际效果选择两类交互：需要解释后果、影响对象、资源消耗或可选范围的操作使用确认
  弹窗；结果单一明确、无需额外选择但误触代价高的直接操作使用长按触发。不得用单一交互方式
  覆盖所有危险操作。
- 确认弹窗描述随具体危险操作变化，由配置表提供文案键及参数；不将可翻译描述烘焙进图片，
  也不在业务代码中硬编码。弹窗仍须明确后果、取消路径和确认动作。
- 长按触发必须在按钮内显示可见进度环，并显示“继续按住以确认”文案；未完成时松开即取消，
  完成后才执行对应操作。

当前仍需继续确认危险操作类型到交互模式／文案键的配置字段、长按持续时间和完成后的成功／失败反馈。
本检查点只冻结方案输入，不授权UI制作、Unity操作、Prefab／代码修改或OpenSpec创建；未占用
ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：ART-006按钮层级、图标文案与设施故障反馈

以下`ART-006`结论已经用户确认或由主美窗口点对点同步，用于约束按钮、图标和跨专业故障反馈：

- 每屏最多一个橙色实色主操作；没有明确主操作的总览页面可以不设置橙色按钮。次要操作使用暖灰
  半实色与深蓝灰边框，危险操作继续使用既定确认弹窗或长按触发，不占用普通主操作语义。
- 正文按钮以明确动作文字为主，图标作为快速识别和降低文字密度的补充。只有空间受限且语义高度
  稳定的工具栏操作可只用图标，并必须提供焦点说明；不得以大量纯文字造成界面拥挤，也不得以图标
  取代关键动作的可理解性。
- 设施故障采用“故障组件本地显著标识＋HUD摘要”：故障传送段、卡住的门或异常接口在世界内以
  红色、图标和受控闪烁节奏定位；HUD摘要说明设施和问题类型。该反馈兼容既定绿／黄／红三级状态，
  并以形状、文字、图标和节奏提供色弱冗余。

当前仍需继续确认图标笔画、工具栏无文字图标清单、故障摘要文案模板及本地显著标识的动效强度。
本检查点只冻结方案输入，不授权UI制作、Unity操作、Prefab／代码修改或OpenSpec创建；未占用
ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：ART-006图标笔画与缩放规则

以下`ART-006`结论已经用户确认，用于约束三级图标美术源、Sprite切片与UI Kit状态变体：

- 图标采用几何线性与轻微圆角：保持工程感、小尺寸轮廓和亲和的拓荒气质；默认保持线性，只有
  激活、资源焦点或严重状态使用局部填充，不转为圆润实心图标体系或完全锐角工业线条。
- 笔画按视觉等重缩放：以`28 px`默认图标约`2 px`视觉笔画为基准，`20 px`层级略细、`36 px`
  层级略粗，但三档的观感与识别稳定性应保持相近。禁止所有尺寸强制同一像素笔画，也不允许每个
  图标无规则地自由调整。

当前仍需继续确认各尺寸的导出笔画值、圆角半径、最小留白和状态填充面积。
本检查点只冻结方案输入，不授权UI制作、Unity操作、Prefab／代码修改或OpenSpec创建；未占用
ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：ART-006状态色语义边界

以下`ART-006`结论已经用户确认，用于约束语义Token、图标状态填充、世界内故障反馈与HUD摘要：

- UI状态色严格复用已冻结语义：绿色表示正常，黄色表示需处理／等待／注意，红色表示故障或
  严重错误；所有状态继续以形状、图标、文字和节奏提供非颜色冗余。
- 橙色只用于推荐主操作、入口和工程功能强调，不表示告警或危险；黄色不与橙色混作异常等级。
- 异星洋红`#C85B98`只保留给资源与特殊事件，不得作为故障或危险状态色，避免与世界资源焦点
  混淆。

当前仍需继续确认各语义Token的实际对比度值、禁用态降饱和规则与特殊事件的HUD优先级。
本检查点只冻结方案输入，不授权UI制作、Unity操作、Prefab／代码修改或OpenSpec创建；未占用
ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：ART-006图标按需说明与七级字体阶梯

以下`ART-006`结论已经用户确认，用于约束图标解释、字体Token、TMP资产和多比例可读性验收：

- 关键操作继续始终显示文字；空间受限且语义稳定的纯图标工具栏操作，在鼠标悬停、键盘焦点或
  手柄焦点时显示简短名称与作用说明。功能理解不得只依赖hover，图标说明也不得替代关键操作文本。
- 字体使用七级阶梯`48 / 40 / 32 / 24 / 18 / 16 / 14 px`：`48 px`仅用于系统级或高关注展示标题，
  `40 px`用于中枢全页标题，`32 px`用于常规页面标题，`24 px`用于区块标题，`18 px`用于正文与
  主要按钮，`16 px`用于次级标签和数值，`14 px`只用于辅助说明。关键操作与状态不得低于`16 px`。
- 七级体系满足GF UI至少五级字号要求；最终以唯一TMP Font Asset、长文本、本地化与`16:9`／`16:10`
  ／`21:9`可读性验收为准，不能仅按1080p设计稿尺寸判断。

当前仍需继续确认中枢标题的最大占高、动态数字的字号降级规则、辅助说明在最小分辨率下的折叠策略。
本检查点只冻结方案输入，不授权UI制作、Unity操作、Prefab／代码修改或OpenSpec创建；未占用
ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：ART-006超宽与16:10适配规则

以下`ART-006`结论已经用户确认，用于补充GF UI多比例验收下的HUD、中枢与面板布局策略：

- `21:9`超宽屏保持`16:9`核心安全阅读带：HUD四角分区与中枢关键控件维持在中心安全区域，新增
  宽度只增加现场视野和留白，不把相互关联的控件拉到物理屏幕两端，也不为超宽屏复制一套页面。
- `16:10`的额外高度优先分配给中枢列表、统计图、滚动区和其他内容区；HUD继续按语义锚定，
  不通过平均拉大控件、按钮或段落之间的间距填满屏幕。

当前仍需继续确认最小分辨率、核心安全边距数值和空间不足时的面板折叠优先级。
本检查点只冻结方案输入，不授权UI制作、Unity操作、Prefab／代码修改或OpenSpec创建；未占用
ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：ART-006空间不足与总览卡片上限

以下`ART-006`结论已经用户确认，用于约束多比例折叠、中枢总览密度和按需展开入口：

- 空间不足时优先保留主状态与主操作，依次折叠辅助说明、次级数值和低频操作；不得通过缩小关键
  文字、隐藏告警或隐藏危险语义来强行塞入全部内容。
- 中枢总览首屏最多显示三张主卡：严重告警／阻塞、当前任务队列、资源与产能摘要。其余内容通过
  “查看全部”进入二级页面，不在首屏动态无限堆叠。
- “查看全部”入口右上方显示折叠的卡片／消息数量`n`；仅当存在折叠内容时显示，并与入口文本、
  图标和焦点说明共同表达，不能只依赖数字徽标。

当前仍需继续确认三张主卡的最小／最大高度、`n`的本地化格式和计数上限、无折叠内容时入口的显示策略。
本检查点只冻结方案输入，不授权UI制作、Unity操作、Prefab／代码修改或OpenSpec创建；未占用
ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：ART-006总览卡片布局与稳定健康态

以下`ART-006`结论已经用户确认，用于确定中枢总览主卡的空间分配及无严重告警时的空状态：

- 总览采用“告警全宽置顶，任务与资源并列”的主卡布局。严重告警／阻塞卡获得完整原因与行动空间；
  当前任务队列与资源／产能摘要在`1920×1080`下并列，在空间不足时上下堆叠。
- 无严重告警时顶部区域保留为紧凑健康卡，显示“运行稳定／无严重告警”、最近一次状态变化及进入
  待处理页的入口；不得隐藏卡片导致布局跳动，也不得保留空白区域。

当前仍需继续确认健康卡的状态图标、最近状态变化的时间格式和健康卡与需处理队列的并存规则。
本检查点只冻结方案输入，不授权UI制作、Unity操作、Prefab／代码修改或OpenSpec创建；未占用
ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：ART-006任务与资源摘要密度

以下`ART-006`结论已经用户确认，用于约束中枢总览下方两张主卡的首屏信息容量：

- 当前任务队列卡首屏最多显示三条任务；每条仅显示任务名称、当前阶段／阻塞状态、简短进度和
  下一步入口，其余任务通过数量提示进入完整队列。
- 资源与产能摘要卡首屏只显示三项当前最影响决策的资源、产能或效率指标及其升降趋势；完整库存、
  资源类型明细和历史数据进入统计页，不以综合健康分数替代具体决策信息。

当前仍需继续确认三项指标的动态选择规则、趋势时间窗和任务进度的文字／图标表达。
本检查点只冻结方案输入，不授权UI制作、Unity操作、Prefab／代码修改或OpenSpec创建；未占用
ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：ART-006资源趋势表达

以下`ART-006`结论已经用户确认，用于补充资源／产能摘要卡和统计页入口的趋势信息表达：

- 资源、产能或效率指标在首屏使用方向箭头和短趋势线；具体时间窗按指标类型配置，完整历史图表
  继续进入统计页，不将大图表嵌入总览主卡。
- 趋势必须以箭头形状、数值变化和颜色共同表达，例如“↑ +12／周期”；颜色只作辅助，色弱、
  减弱动态或低饱和状态下仍能判断方向与变化量。

当前仍需继续确认各指标时间窗、变化值单位／精度和趋势不确定或数据不足时的降级状态。
本检查点只冻结方案输入，不授权UI制作、Unity操作、Prefab／代码修改或OpenSpec创建；未占用
ArtResource 8091、DCC、文件写锁或Git索引。

### 2026-08-24：ART-006数据不足与任务进度降级

以下`ART-006`结论已经用户确认，用于约束趋势摘要和任务队列在不完整或非连续数据下的诚实表达：

- 趋势数据不足时显示“数据采集不足”与中性占位，并保留统计页入口；不得伪造“稳定”、升降趋势，
  也不得直接隐藏会影响理解的指标。
- 任务进度以阶段文字优先，配合状态图标；只有存在真实`0～1`可量化进度时才显示细进度条。对位、
  等待资源、阻塞等阶段型任务使用明确文字，不以伪精确条形进度误导玩家。

当前仍需继续确认中性占位图形、数据不足的刷新触发条件、进度条最小可见变化和阶段文案模板。
本检查点只冻结方案输入，不授权UI制作、Unity操作、Prefab／代码修改或OpenSpec创建；未占用
ArtResource 8091、DCC、文件写锁或Git索引。
