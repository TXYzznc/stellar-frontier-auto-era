# P2-007 传感器服务实施准备

2026-09-11，用户要求客户端在UI等待期间继续其它任务。任务ID `client-p2007-sensor-preparation`，priority10。独立于挂起的B11，不重开B12/B13。

当前授权为下一实施单元的只读核对与技术方案准备，不擅自实现新系统。唯一写入交付：`Docs/Development/Dispatch/Planning/P2-007-Sensor-Readiness-20260911.md`。队列使用正式工具；不改代码、资源、数据、任务表/xlsx、Git或驱动Unity。

依据只读任务表 P2-007「实现传感器基类与绑定目标读取」，依赖P2-002/P1-001/P2-006，核对B10/B11/B12/B13现存实现和完整现行设计，确认实际缺口，给出最小可独立验收实施方案与精确写路径。按项目推荐专业技能执行；不得只核对队列后停止。

重点：绑定永久对象引用；只有一个目标通道的既定类型规则；休眠/断电/离距/目标删除/算力不足时有效性、原因、事件与旧数据处理；距离从组件锚点到有效区域；算力持续租约与释放；只读取目标公开数据，不泄露应由玩家决定的阈值或自动决策。逐土地湿度与生长数据以稳定单元ID关联，不平均化整田；空地湿度独立于播种。扫描/通信不扩张为第一版真实探索或通信调度。

未完成农田/资源生产不作为造假数据的理由：明确已有公开提供者、尚缺的只读接口与仅测试夹具，区分传感器基础服务完成和完整资源传感表现。复用既有调度、名册和区域服务，不另造算法执行器/UI/Shader。

交付包括目标/非目标、依赖实况、窄方案、生命周期、自动验收、精确文件候选、尚需决策；继承已确认规则，不重复询问默认行为。列明后续OpenSpec建议名称和规模，但在方案明确确认前不创建新change或实施代码。若无需新产品决策，明确说明，只提交整体实施建议供制作人收口。交付后实际通知制作人，再complete --claim-next；不得自动Git。
