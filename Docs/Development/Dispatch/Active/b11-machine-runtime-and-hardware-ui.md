# B11 机器运行与硬件UI并行派发

2026-09-10用户明确启动后续工作，已确认机器UI优先、整套交互同批、视觉通过后连续素材/Prefab生产。OpenSpec：b11-machine-runtime-and-hardware-ui。

## 客户端：b11-client-machine-runtime

### 2026-09-11整包V02视觉通过，授权正式迁入与运行接线

用户查看制作人列示的硬件、概况、选择器、确认弹窗实际截图及未覆盖项后，对“通过本包视觉候选，并允许正式迁入接线”明确回复“确认”。完整交接入口为 ArtResource/Docs/ArtPipeline/UIRequirements/B11_MachineHardware/AssemblyReview_V02.md，精确清单为同目录 CandidateManifest_V02.json，审计为 AssemblyAudit_V02.json。此前等待用户最终整包验收和完整交付的条件已解除，不再重复索要效果图或Prefab视觉通过。

恢复本任务4.x：仅迁移B11_MachineHardwarePanel_V02及清单中实际必需的已通过依赖，保持GUID并核验冲突；正式路径沿用下文Operations Prefab与MachineHardware Sprite范围。不迁移验证Canvas、19张证据图、静态夹具脚本、旧V01或其它过程资产；不覆盖共享字体，由用户后续处理。新旧入口及依赖先盘点，禁止覆盖无关资产。

28个空回调、示例数值、运行时输入/滚动/焦点、权限与实例ID、装卸影响预览、异步失效和生命周期仍由客户端实现并验收，不把视觉通过写成运行验收通过。特别核对主面板及异常状态的既定退出方式，确保不困住玩家；复用既有输入抽象/关闭合同，不另设计整套UI。对象图标仍不在本批美术生产范围。按既有QA门禁核验正常编译、引用、Console和真实操作；新增视觉改动仅交美术复核增量。无真实外部阻塞时连续实施，整包完成实际回传制作人，不自动Git，不改任务表。

### 2026-09-11当前等待条件

效果图已获用户批准，客户端仅待完整B11 UI素材/Prefab交付后恢复4.x；无需再次等待效果图批准、DEC195或编译。B14已独立交付，不重跑。现有队列工具不支持原位修订已blocked条目的说明，因此本机历史blockReason/resumeWhen可能仍含旧视觉等待措辞；以本节及最新交付证据为准。保留blocked和检查点，不为修说明先unblock/重新领取，不直接编辑队列JSON，也不新增队列工具开发任务。真实完整交付到位后再按正常unblock流程处理。

执行tasks 1、2、4的客户端项，先P0-011配置再P2-001～006再P2-009/010接线。不是只读盘点包：当前已授权开始实现；盘点完成有安全下一步即继续，不请求逐段放行。
先读本change全文及现行设计，client-baseline记录准确类型/文件/数据ID。两类载体与货舱容量效应器遵循当前设计，不搬用历史固定载体无通用容器、统一2/2/1等旧口径。
允许主仓库 `Assets/Game/Scripts/AutoEra/`、`Assets/Game/Tests/AutoEra/` 本次增量；本change evidence/client-*.md。按职责复用旧代码，无包/asmdef扩张。
数据允许 `GameData/AIData/{DataTables,Configs,Languages}/Machines/` 及本批完整对象目录所需 `Catalog/` JSON；先检已有类别，已有同义类别复用而不另建双份。GenerationProfiles仅必要映射；正式xlsx/TXT/bytes/C#只由安全工具产生到对应业务类别与AutoEra/DataTable，任务表只读。不得直接手改生成代码。AppConfigs仅必要新业务配置登记，Core既有项不删改。
场景/资源：允许 `Assets/Game/Prefabs/Entity/Machines/` 本次机器逻辑候选、`Assets/Game/Scene/MachineManagementValidation.unity` 独立集成验证入口及meta；先核实是否已有等价正式入口并复用，禁止覆盖同名非本包资源。
已批准UI交付后允许 `Assets/Game/Prefabs/UI/Operations/` 必要增量接线，以及 `Assets/Game/Sprites/UI/Operations/MachineHardware/` 必要新Sprite；资源依赖meta/GUID明确验证。美术未通过时只做业务和测试，不重画整套Form。正式素材迁移只复制用户通过的依赖，不导入过程图。
缺失静态美术以明确开发代理或不展示图标进行服务验证，不冒充正式交付；不得重开B08。未实现能源/信号使用清楚标记的适配测试入口，不新写电网/导航/完整算法/生产物流/磁盘存档。框架核心新增修改须精确报制作人，不自动继承B10八文件修复授权。
与2D直接对齐字段/意图/拒绝原因，与QA协调8090。冻结短机械任务按RapidExecution直接拆包。整包完成实际通知制作人，提供通过和未覆盖证据，不操作Git/任务表。

## 2D：b11-art2d-machine-hardware-ui

### 最新裁决：2026-09-11配套效果图通过，恢复拆图；字体由用户后续处理

用户明确通过 Supporting_20260911 的 Overview_V01.png、ComponentPicker_V02.png、Feedback_V02.png，并要求美术拆图。连同此前通过的硬件页，按原B11范围连续完成拆分、必要素材、候选装配及自验；无需再请求本批效果图确认，不扩大到其它UI家族。

字体不再是本批阻塞：不回滚、不清理、不重建共享字体，不做字体优化或覆盖主工程字体；保持现状并在交付依赖清单标明，由用户后续统一处理。既有哈希变化仅保留事实，不重复索要决定。

拆分独立可复用背景/边框、按钮及状态、槽卡、进度条与必要小图形；普通文字、动态数值/状态、对象图标均不烘焙。不要拿整张效果图或不透明裁切补丁冒充可交互资源；按适用拆图技能核验透明边缘、串图、状态一致性和清单数量。保留已认可外观，不重新探索风格。槽内传感器/效应器/核心图标仍不在本次制作范围。

采用本派发已授权ArtResource路径及资源规范，不采用技能范例中的Resources等旧目录；复用可用导入工具，不为技能过时工具名新增无关工程。只强制1920×1080候选验证，源素材尺寸按使用需要如实记录。确认反馈需预留具体影响信息，四反馈状态互斥，补齐滚动/空/加载/失效状态。

完成完整资源与候选自验后实际通知制作人和客户端；在完整交付到位前不解锁B11客户端接线，不自动迁移主工程或Git提交。

### 最新裁决：2026-09-11合并硬件页通过，继续配套交付

用户已确认 `Representative_20260911/D_Merged_NoObjectIcons_V02.png` 的造型与布局：图三机械外框＋图一2–1–2五槽。此节优先于下方历史三方案与等待选择条款，不重新探索风格。

- 该图实际1672×941，仅为已通过视觉基线；正式候选/验收统一1920×1080，去除右下制作说明。文字、状态与对象图标为运行时控件，不烘焙进背景。
- 传感器/效应器/核心对象插画不属于本包，不生成；保留动态图标容器，空槽加号/选择入口按合同实现。
- 解除本次合并图等待，继续既有B11范围：复用已通过视觉制作硬件页必要素材与候选；完成机器概况、本批组件选择及安装/拆卸反馈的配套设计。尚未获用户确认的配套页/新增外观仍先交制作人检查并由用户决定，不把本图通过解释成所有页面通过。
- 已通过单元可连续切图、装配与自验，无须再次请求普通开工；独立缺失视觉可并行准备，不以单页等待堵塞已授权工作。整包实际完成后通知制作人与客户端，才能解除客户端完整交付依赖。
- 原ArtResource写入范围不变，不写主工程、C#、Git或xlsx；不重画ART006、不展开其它UI家族。

### 当前执行：2026-09-11按已确认家族制作机器硬件代表页

用户确认18组复用分组后要求继续推进，不再让B10等待阻塞UI工作。本节覆盖下面历史停止与旧三方案：恢复现有B11的视觉设计单元，优先05机器管理；先选“现场机器硬件页”作为同内容代表页制作三种完整美术候选，非四场景各三张，也非18组全量生产。

- 先实际打开已验收基地中枢效果图作为风格参照，在记录中写精确路径。继承材质质感、工业科幻气质、字体/状态色语义和完成度；不照搬象牙白终端壳，不再用只有细线和卡片的布局草图代替游戏美术。
- 三方向使用此前用户认可的外观语言：嵌入式设备面板、模块化检修面板、轻型仪表面板。分别有完整轮廓、材质、边缘厚度/分层、定制槽卡/按钮和克制机械细节。必须使用适用图像生成技能/工具实际绘制，不用脚本矩形线条拼图冒充原画。
- 同一轮式机器、同一硬件状态、同一五槽和字段/操作集合、相同背景输入；允许不同布局。简要身份/状态头不是新增合并页面，保留既有概况/硬件切页合同；不为模板展示添加未授权玩法。
- 仅三张代表候选，三张齐全并实际自检后交制作人报告、用户选择。12选择器和17确认组仅记录对接需求，本轮不另画整套；其他家族先不出图。目标1920×1080，实际像素如实记录。
- 原授权ArtResource路径内使用新目录 `Representative_20260911/` 保留源图和比较说明；旧rejected不覆盖。同步InteractionContract与ReviewPackage的历史造型限制，不能只通知不做。未获新视觉通过不切图、Prefab、Unity或主工程接线，无Git/xlsx。

后续规划顺序：05机器管理代表页→本批12选择与17确认的场景化配套→既有B11完整交付；再按业务准备情况安排09资产目录与10交易/图纸、06资源/07建筑/08工坊、13算法工作台等。该后续规划不是全量生产授权，03/04已验收基线不重画。

立即执行tasks3完整批次：机器概况、硬件槽位、组件选择、安装/拆卸结果/拒绝/等待完整流程。先读现行功能文档，与客户端对齐，不等待业务代码完工再设计。
允许ArtResource `Docs/ArtPipeline/UIRequirements/B11_MachineHardware/` 文档和预览、`ArtSource/UI/B11_MachineHardware/` 源图、`Assets/Art/Authoring/UI/MachineHardware/` 候选与验证、`Assets/Art/Delivery/UI/MachineHardware/` 已验收交付；按资源规范建立子目录。
继承ART006字体、状态色语义与交互语言，不直接沿用基地中枢的象牙白实体终端外壳；机器现场UI按下述2026-09-11用户决定探索差异化外观。正式一级导航不变，1920×1080唯一强制基准。主工程Prefab/C#/配置不可写。
先完成交互合同/状态矩阵与效果图整包，实际通知制作人检查结构功能，再由用户视觉决定；不得直接跳过制作人向用户求验收。缺关键需求先查现行设计，不把通用规则重新问三轮。
用户通过后连续完成必要切图/候选装配/状态自验，不逐阶段再等开工消息；没有新素材直接复用。客户端单独负责主工程接入和业务绑定，美术复核新增部分。
8091按需使用，无需为客户端8090停工；如实际共享资源冲突才协调。单张/单页完成不暂停，整批结束通知制作人与客户端并claim-next。

## 2026-09-11 用户确认：机器UI三方案对比返修

> 后续裁决覆盖：本节三图已交付并被用户否决，不能继续作为生产方向。用户已确认[第一版界面清单与复用分组](../../../GameDesign/04-第一版/08-界面清单与复用分组.md)，按家族规划差异化外观；原四图及本节三图均保留历史。四场景×三版的12张提议未授权，不执行。新代表页范围确认前不盲目重绘、切图或Prefab；完整B11仍未完成。

原四张候选因误用基地中枢象牙白外壳被用户否决，保留为历史，不用于生产。用户已确认三方向、同一内容对比，以及允许探索布局；恢复原 `b11-art2d-machine-hardware-ui` 的设计部分，不代表视觉已通过。

- 三张独立候选：轻量战术HUD（深色半透明底、细线）；工业控制面板（深灰实底、紧凑模块）；极简科技卡片（轻薄悬浮卡片、较多留白）。禁止三张仅换色，禁止复制中枢实体外壳。
- 三图均呈现同一“机器概况＋硬件槽位”比较场景，使用同一机器、同一数值、同一状态、同一背景视角及相同功能集合；允许面板位置、分区、按钮造型不同。该合并比较场景不擅自冻结正式页面导航。
- 保留现有字体及状态色含义，功能、权限、组件实例与槽位合同不变。轮式载体两传感器、一核心、两效应器须完整可辨，不混入固定载体说明。数值从当前合同核对，不新增平衡规则。
- 只制作三方案效果图及必要比较说明；目标1920×1080，不做多比例。记录实际像素尺寸，生成工具未达到时如实标注，不冒充最终Prefab截图。
- 在原已授权ArtResource目录建立独立比较版本，更新InteractionContract/ReviewPackage/检查点中冲突的旧造型与固定布局要求；旧图不覆盖，不改已验收ART006。此轮不切图、不制作Prefab、不驱动Unity、不写代码或Git。
- 自行实际打开三图，检查同内容可比性、可读性、槽位与权限。三张全部完成后实际回传制作人检查结构功能，由制作人向用户汇报，用户决定选择/混合/返工。单张完成不停止，未获选择不扩展其余视图。

## 后续2D独立包：b11-art2d-algorithm-editor-design-preparation


排在机器UI完整交付之后，不抢占。仅允许ArtResource `Docs/ArtPipeline/UIRequirements/AlgorithmEditor/`：盘点P3-010～012/014、现行强类型端口与用户已确认节点规则、已有素材和缺项；给出画布/节点库/检查器/模板/调试的设计边界与批次建议。
此包仅设计准备文档，不生成效果图/切图/Prefab，不写算法程序。与客户端核对真实接口，交制作人后建立后续生产设计范围，不把机器硬件批次永久延长；尚无接口则记依赖而不是虚构。

## 批次共同门禁

### 2026-09-10 配置缺值已获用户确认

DEC-195及数值模型13、容量规格14为正式依据：固定载体一级/二级HP100/120、基础容量10/12、待机0.1/0.1、工作1/1.2；货舱一级/二级增加容量30/36、功率均0/0，一级购买400/回收320，升级不提高回收价。客户端可解除相应阻塞，按原范围完成JSON安全生成、GF读回及正式资源实例验证，不手改xlsx/生成物、不新增经济或升级系统。UI视觉依赖仍未解除；配置单元做完若只剩UI等待，再block让位，不勾选完整B11。不要将合法0功率误当缺配置；缺值状态与数值0需分离。

完成必须实际回传制作人；普通进度不等待已阅。Git仅用户手动触发，不自行暂存/提交/唤醒Git。任务表本批只读，三轮用户确认不扩大为随意重构/改框架授权。
