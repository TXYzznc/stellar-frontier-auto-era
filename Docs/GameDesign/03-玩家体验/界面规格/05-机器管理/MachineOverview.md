# 机器现场概况

状态：2026-09-19设计候选，供18项完成后统一验收。页面ID：`MachineOverview`；家族：05-机器管理。

## 目的、入口与归属

玩家任务：机器现场概况。入口：现场选中机器；硬件等分页返回概况。

所属 `FieldHudForm`，页面根 `Panel_PageMachineOverview`。本页为该Form内预制内容区；同Form普通页互斥，HUD模块除外。
完整节点树、组件与逐节点RectTransform见本家族[prefab-layout](prefab-layout.md)的MachineOverview节；外壳、导航、关闭节点见[共享外壳](../00-共享外壳-prefab-layout.md)。

## 结构与布局

内容范围 520×832。右侧距屏幕右24、顶部120；分区自上至下排列，保留世界可见空间。

| 分区 | 展示字段 | 区域职责 |
|---|---|---|
| 身份与开关 | 名称／型号；激活、运行、供电、连接状态；主任务／等待原因；算法异常 | Identity |
| 容量与运行概况 | 算力；逻辑容量；功率；货舱货物与容量；传感器／效应器运行概况 | Capacity |

分区根为Panel_MachineOverview加区域英文Key；每区声明标题、滚动正文和默认inactive的真实条目模板。
文本独立排版，不烘焙入图片。暂无数值使用“—”；说明与历史记录不伪装成实时测量。

## 可交互对象与效果

| 控件／意图 | 标签 | 条件、操作与效果 |
|---|---|---|
| Btn_MachineOverviewActivate | 激活或重新激活 | 满足现场条件和激活服务检查后执行，不能远程首次激活 |
| Btn_MachineOverviewSleep | 休眠或开机 | 切换整机请求运行状态，等待安全点时显示等待原因 |
| Btn_MachineOverviewPower | 断电或恢复供电 | 按供电管理合同执行，影响未完成行为时先确认 |
| Btn_MachineOverviewRename | 重命名 | 打开17-重命名 |
| Btn_MachineOverviewFocus | 镜头聚焦 | 聚焦当前机器 |
| Btn_MachineOverviewHardware | 硬件 | 切换05-硬件 |
| Btn_MachineOverviewAlgorithm | 算法 | 切换05-算法 |
| Btn_MachineOverviewDiagnostic | 任务与诊断 | 切换05-任务与诊断 |
| Btn_MachineOverviewRecover | 回收机器 | 打开17-机器回收确认，保留组件配置回到未部署库 |


## 继承规则与本页边界

现场离开阈值后降级为远程权限并解释，关闭后不自动发操作。无手动播种、浇水、采集、运输按钮。计算核心无独立开关。

业务来源：[01-机器自动化与算法](../../../02-系统设计/01-机器自动化与算法.md)；所有页面继承[通用合同](../00-通用合同.md)及[页面关系与复用](../00-页面关系与复用.md)。已有规则直接复用，具体排版及新增入口是本次候选设计。

## 数据、权限、返回与状态

- 数据为相关领域服务提供的只读快照：身份与开关、容量与运行概况；打开首次取数，领域事件增量更新。历史阅读锁定对应记录与版本。
- 参数保留来源与返回上下文，传稳定对象／槽位／算法／记录ID；失效或无权限时禁用写操作并说明，不自动按名字替换对象。
- 返回先关闭选择或确认，再退详情／子页，最后回入口；恢复分页、选择、筛选、滚动与焦点。已提交等待继续按业务请求状态运行。
- 首焦点优先取消／返回，否则首个可用安全动作；公共关闭按钮在导航末尾；纯展示内容不抢焦点。
- default、focused、pressed使用Selectable过渡；disabled解释具体原因；loading反映真实读取；empty给无对象／无记录说明；error保留上下文及恢复路径；success只由权威结果触发。只读文本不适用pressed，未发提交不伪造success。
- 特殊边界：现场离开阈值后降级为远程权限并解释，关闭后不自动发操作。
- 失败恢复：修正缺失条件再提交；业务请求结果不确定时继续等待，不允许UI自行超时重发。成功后仅刷新该对象与受影响摘要，不重置无关页面状态。

## 接入、素材与验收

Form使用AutoEra.UI命名空间及项目基类。每个按钮序列化字段采用camelCase并绑定本文布局内同名完整路径；输入、Button和快捷键汇入同一意图。OnClose释放页面读取和监听；已接受事务不随UI销毁而取消。时间、单位、价格和本地化由显示层格式化服务提供。
所需素材：容器底板、按钮正常／焦点／按下／禁用表现、类型图标、状态图标；图文页另需可选示意图。结构期用通用token和明确占位Image，中枢外壳复用已验收资源。字体及射线约定见通用合同。
文档自验：分区、数据、控件、返回、权限、状态均有说明；不得把页面文件数量当Form数量。未来原型需GF标准07的L1—L4检查、项目检查及1920×1080结构截图，本次只交设计文档。

## 变更记录

- 2026-09-19：用户批准18项顺序全量设计；首版布局和具体交互待集中验收。
