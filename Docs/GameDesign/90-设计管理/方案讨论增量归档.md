# 方案讨论增量归档

> 状态：持续维护
> 最近更新：2026-08-21

本文件保存尚未进入或跨越多个 OpenSpec 的已确认方案检查点，防止长讨论中的
上下文在集中落盘前丢失。它不保存原始聊天流水，不替代对应系统文档、决策记录、
开发任务表或 OpenSpec。

## 增量归档规则

- 连续方案讨论每累计两轮，必须至少完成一次增量归档；重大边界、不可逆选择或
  跨项目契约确认后立即归档，不等待整组讨论结束。
- 每次归档至少记录覆盖范围、已确认结论、明确排除、待决事项和影响的任务／文档。
- 稳定结论同时同步到对应系统文档和[决策记录](决策记录.md)；当前完成度同步到
  [设计状态总表](设计状态总表.md)；未决事项同步到
  [开放问题与讨论顺序](开放问题与讨论顺序.md)。
- 增量归档只保存阶段性共识，不构成 OpenSpec artifact 创建授权。每个 OpenSpec
  仍须完成最终决策摘要并取得用户明确确认。

## 2026-08-20：ART-001～ART-003 视觉与场景方案检查点

### 已确认

- `ArtResource` 是美术源文件与美术产出的唯一来源；主项目只接收可用资源并完成
  业务绑定，修改必须回流美术项目。
- 视觉预研先制作三组维度完整的候选方向，统一覆盖环境、机器、建筑、色彩与材质，
  经统一标准比较后选定或融合为一套正式视觉基线。
- `ArtResource` 建立独立 LookDev 场景，统一相机、灯光、后处理、尺度参照和资源
  展示方式；主项目只做最终接入验证。
- 本批次使用最小 `.unitypackage` 验证交付合同；正式量产阶段的长期同步方案另行设计。
- 场景路线使用同一代表性测试区轻量比较 Terrain、模块化、手工布景及混合方案，
  统一评估视觉、制作效率、复用、性能和运行扩展，再深化胜出方案。
- 总体风格确定为明亮、偏卡通化的风格化中模 3D：使用简化 PBR、清晰轮廓与
  功能色块，有必要细节但不追求写实微表面，不使用明显漫画描边作为默认方案。
- 第一版采用“人工固定玩法骨架＋编辑器程序化生成视觉环境＋烘焙后人工修整”。
  地表分区、植被、碎石和非交互装饰可程序生成；基地、道路、资源点和通行空间
  由设计确定。第一版不做每存档随机地图或无限区块。
- 美术生产采用模块化资产库与共享材质体系；DCC 可编辑源文件和 Unity 交付资源
  分离；单个资源按“概念／轮廓—Blockout—动作与接口验证—中模材质—LookDev—
  Prefab／LOD／导入”的阶段门禁制作。

### 明确排除与待决

- 第一版不制作动物，也不建设动物程序动画管线。
- 可视化动作节点编辑器是待定增强项，不是当前动作管线首阶段验收条件。
- 精确环境密度、材质预算和生成工具参数在 ART-003 样片与 Profile 后确定。

### OpenSpec 授权与落盘

- 用户于 2026-08-21 明确确认 ART-001～ART-003 最终决策摘要，授权创建
  `b02-parallel-art-art001-art003-visual-foundations`。
- change 已完成 proposal、design、三份 capability spec 与 tasks，分别覆盖独立美术
  项目交付合同、统一视觉基线和固定玩法骨架／编辑器视觉生成的场景路线。
- 本次授权只覆盖 OpenSpec artifact 创建，不代表资源制作已经开始；进入 apply 前仍由
  用户明确启动实施。

## 2026-08-21：ART-001完成与ART-002基座检查点

### 已完成

- ART-001交付合同冻结为1.0.0；最小包包含2个Prefab、6个URP/Lit材质和1个关节JSON。
- 主工程首次导入后9/9 GUID一致；同版本重复导入后9个文件哈希和主工程验证场景哈希均不变，没有重复身份或绑定丢失。
- 为避免重复导入在旧路径重建资产，交付包内路径直接镜像主工程最终类型路径；交付资产不在首次导入后迁移。
- ART-002已建立统一LookDev基座，固定代表对象、High Fidelity质量档、1920×1080输出、三档评审相机、中性灯光与后处理。

### 待用户确认

- 用户已确认A“清朗拓荒工程”、B“异星生态高彩”、C“工业图形块面”三组候选主题；三组统一条件样片、状态样本、9张固定视角截图和第一轮评分矩阵已完成。
- 用户确认以A为综合色彩与材质基线，吸收B的局部异星资源高彩和C的机器功能分区／安全标识；融合职责、Do／Don't、调色板、材质、轮廓、灯光与细节层级已冻结为唯一正式视觉基线。
- 正式融合场景、17个材质和Far／Mid／Near三张最终图已生成。初检发现异星青绿误复用于普通植被后已修正：普通植被恢复A自然绿，B高彩只保留在资源点邻近生态。

## 2026-08-21：ART-003代表性测试区检查点

### 已确认布局

- ART-003只制作同范围路线对照样片，不承担正式第一版地图设计；四条路线必须共享
  同一玩法对象变换、净空、固定相机和灯光条件。
- 测试区约48×40米。基地侧包含8×6米基础仓库和8×6米制造工坊，资源侧包含
  一个10×8米权威矿山，并保留普通自然环境区与明确的可建造空地。
- 一条4米宽主通行路线连接仓库、工坊和矿山，覆盖直行、转弯、作业接近与装卸
  接近的代表性空间关系。
- 初始视觉生成净空为：建筑占地外扩1米、道路生成禁区宽4米、作业点半径2.5米、
  物流点与等待点半径2米。后续样片发现载体通行或俯视可读性不足时，可形成证据后
  再提请调整。
- 仓库、工坊、矿山、道路、作业点、物流点、等待点、建筑占地、导航空间和可建造区
  都属于固定玩法骨架；视觉生成工具不得移动、删除或侵占。
- 矿山主体是唯一权威资源对象，周边矿石碎片、晶体和异星生态只是视觉实例，不具有
  独立玩法ID、产量、库存、导航或存档权威。

### 实施与验收边界

- 固定骨架作为独立、可重复构建的ART-003场景保存，不修改ART-002正式视觉基线。
- 完成骨架后先提供固定俯视截图与场景查看入口，再以此为母版开展Terrain、模块化、
  手工布景和混合方式的轻量试制。

### 2026-08-21实施进展

- 四路线已在同一骨架上完成轻量试制。Terrain适合连续地表与缓坡；4米模块铺满
  48×40米区域即产生120个地表单元，接缝与对象数代价明显；手工路线构图直接但
  扩区成本最高；混合路线未触发重评条件。
- 混合职责进一步固定：Terrain只负责连续地表，模块负责道路／平台／资源点硬边，
  Editor生成负责植被、碎石和资源外围碎片，人工只负责焦点构图与生成后修整。
- ArtResource未安装ProBuilder；样片模块使用Unity原生网格原型，不为路线验证新增包。
  正式模块仍由DCC制作并按ART-001合同交付。
- Editor生成器位于美术项目`Assets/Editor`，按`VISREG_*`授权区域和`EXCL_*`净空
  工作，不创建玩法组件或逐实例ID。当前108个生成装饰通过0净空违规断言。
- 保存重开、人工标记保留、未选区域手动移动保留、选区重生成均已验证；混合路线
  已生成日间与夜间Far／Mid／Near证据。
- 性能采样首次执行时被8092的Auto模式安全门拦截；未绕过限制，也未在拦截时进入
  Play Mode，等待用户明确切换Bypass后才继续。
- 用户将8092切换为Bypass后，混合日间样片已完成三次Play Mode瞬时采样并立即退出。
  条件为High Fidelity、4×MSAA、URP-HighFidelity、固定Far相机、GTX 1650。
- 三次读数一致：291个GameObject、223个MeshRenderer、306,187三角形、790 batches／
  draw calls、15 SetPass、390个阴影投射计数，动态／静态／实例化批处理均为0；
  总分配内存约214.36MB。
- 当前小样片未显示帧时间阻塞，但Editor瞬时读数不冻结最终预算。明确瓶颈是Primitive
  多Renderer、未批处理和大量阴影投射；正式资产须进行Renderer合并、实例化、LOD、
  阴影分级，并在独立Player与目标密度下重新Profile。
- ART-003对照报告、资产需求、日夜证据、持久化证据、性能证据和路线职责已经齐备，
  当前只等待用户完成方向确认。

### 2026-08-21最终确认与移交

- 用户确认ART-003方向通过；ART-001、ART-002、ART-003至此全部完成用户验收。
- 场景路线正式冻结为混合职责：Terrain承接连续地表和缓坡，模块承接道路／平台／
  资源点硬边，Editor生成承接授权区内无玩法身份的背景装饰，手工布景承接焦点构图
  和生成后修整。
- 本批没有引入运行时随机地图、动物动画、Motion Core／Editor、AutoEra Adapter、
  正式批量资源或框架核心修改。
- 性能样片支持继续使用混合路线，但790 draw calls、390个阴影投射计数和无批处理
  明确要求正式资产进行Renderer合并、实例化、LOD、阴影分级，并以独立Player重测。
- 最终密度、材质数、LOD距离和毫秒预算不在本批伪造冻结，转交ART-004及正式代表
  资产性能门。
- 美术项目已形成`Docs/ArtPipeline/ART-001-ART-003-Handoff.md`移交包。任务表继续保持
  AI只读；建议用户将ART-001～ART-003状态手动改为“已完成”，实际工时按本人记录填写。
- 联合边界审计发现ART-001早期交付路径多加了一层`AutoEra`。两边已同步改为按资源
  类型下的`ContractSample`业务类别组织，验证场景移至`Scene/ArtValidation`；最终0.1.1包
  连续导入两次后9个资产与验证场景哈希不变，GUID／Prefab引用保持有效，边界审计恢复通过。

## 2026-08-20～2026-08-21：程序化动作管线检查点

### 已确认架构

- 第一版机器主体动作采用程序化刚性关节路线。机器从 Blockout 阶段即按可动部件、
  正确 Pivot／轴向、关节范围、Socket、作业点和安全姿态制作；不先做整机成品再切件。
- 管线核心抽象为“运动原语＋受控动作组合”。基础原语覆盖旋转、伸缩、瞄准、开合、
  持续旋转、往复和等待；组合支持顺序、并行、循环、条件等待和有限分支，不允许
  在动作资产中嵌入任意脚本。
- 玩法逻辑是唯一权威。动作管线只读取状态、阶段、目标与归一化进度并负责表现，
  不以动作播放结束决定产出、任务完成或存档结果；离线返回不补播历史动画。
- 机器主体使用运动学刚性关节：独立 Transform、局部轴、范围、默认姿态、速度和
  加速度限制。物理关节不负责主体驱动，逻辑根、导航和碰撞权威不随视觉关节移动。
- 动作具有准备、运行、完成、取消和恢复生命周期；同一关节通道独占，切换从当前
  实际姿态平滑进入新动作。中断策略按动作配置为保持、收回、归零或立即停止。
- 配置分为两层：Prefab 上的 `MotionRig` 绑定稳定关节 ID 与 Transform；独立
  `MotionGraphAsset` 保存动作定义。ScriptableObject 只保存静态配置，不保存运行状态。
- `MotionGraphAsset` 使用带版本号、稳定节点 ID、强类型参数声明、显式节点表与连接表
  的结构；不直接序列化场景 Transform、玩法组件或运行时状态。
- 动作参数通过强类型上下文注入；主项目 `AutoEra Adapter` 把真实玩法状态转换为
  参数，美术项目使用模拟上下文预览。动态目标传递 Pose 或目标提供接口，不在动作
  资产中保存业务场景引用。
- 每个 Rig 使用一个集中式 `MotionExecutor` Tick，不为每个关节建立 Update，不以
  协程或 DOTween Sequence 作为执行核心，运行热路径避免持续 GC 分配。
- 作业动作使用玩法状态／进度，待机、轮转、扫描和状态灯等纯表现循环使用本地表现
  时间；重进场景、对象池复用或离线返回时从当前玩法状态重建姿态。
- 表现按可见性与距离分级：近景完整求解，中景降频插值，远景保留关键姿态，不可见
  或卸载时停止表现 Tick；降级不得影响玩法、碰撞、产出和存档。

### 跨项目与首阶段范围

- 主项目是动作管线唯一代码源。逻辑上分为无 GF_X／玩法依赖的 Motion Core、只在
  Editor 运行的 Motion Editor，以及仅主项目拥有的 AutoEra Adapter。
- Core 与 Editor 通过固定 `.meta`／GUID 的版本化 `.unitypackage` 发布到 `ArtResource`；
  美术项目只读使用，不修改工具源码。动作工具包与美术资源交付包分开发布。
- 首阶段以一台轮式载体、一个多关节机械臂和一个可替换效应器完成纵向贯通，覆盖
  待机、移动、转向、作业、取消、断电、安全收回、恢复、对象池复用和重进场景。
- 验收分为静态校验、EditMode 执行器测试、PlayMode 代表性样机、跨项目 GUID／引用
  验证和性能冒烟；精确并发数量与毫秒预算在真实样机 Profile 后确定。

### 任务落表与校验

- 用户已将 `P0-014`～`P0-016` 补入任务表，并同步 `ART-031`、`ART-G0-001`
  与 `ART-027` 的阶段门依赖；任务表永久保持 AI 只读。
- 2026-08-21 只读复核通过：任务表共 169 项，程序预算 122 项、
  1232～1920 小时，美术资源预算 47 项、468～730 小时；无重复任务 ID、
  缺失依赖、自依赖、依赖环或超过 18 小时的单项任务。

### 仍待完成

- 物理程序集划分会影响现有 Hotfix、HybridCLR 与 FSR 配置，须在对应程序 OpenSpec
  中核对并明确，确认前不修改 asmdef 或框架核心。
- 可视化节点编辑器是否进入后续任务，等待 Inspector 配置与代表性样机验证后决定。

## 2026-08-21：项目功能验证中心第一检查点

### 已确认

- 建立一个长期使用的 AutoEra 项目级功能验证中心。以后每完成一个适合人工触发与
  观察的功能，都为其增加对应的按钮、配置项、执行结果和必要的重置入口。
- 验证中心采用单一 EditorWindow，同时服务 EditMode 与 PlayMode；每个验证模块声明
  自己允许或要求的运行模式，窗口根据当前 Editor 状态控制可用性。
- 各功能以独立验证模块接入，不把所有按钮和业务代码持续堆积到窗口主类中。
- 窗口使用 Unity Editor 的 `TypeCache` 自动发现验证模块，并按稳定元数据分类、排序；
  新功能只需新增模块，不需要同步修改中央注册表。
- 验证模块的临时／上次使用参数保存在开发者本地，不提交 Git；需要稳定复现与团队
  共享的标准配置可由用户主动保存为具名项目预设并纳入版本管理。
- 各模块自行声明验证环境：纯逻辑功能可直接执行，场景相关功能可使用当前场景或要求
  专用验证场景；运行条件不满足时，窗口必须禁用执行并说明缺失条件。
- 对当前场景的修改必须接入 Unity Undo；模块创建的临时对象必须提供明确的重置或
  清理路径，避免污染正在制作的正式场景。
- 验证中心属于 AutoEra 产品工具，只能进入 `Assets/Game/Scripts/AutoEra/Editor/`，
  不修改框架核心，也不进入 Player 构建。
- 验证中心用于快速人工验收，不替代 EditMode／PlayMode 自动化测试；面板调用正式业务
  接口或测试适配器，不复制一套业务实现。
- 验证中心只强制覆盖适合反复配置、主动触发并观察运行效果的程序功能；文档、纯资源、
  一次性交付管线、底层重构和不适合交互触发的任务不为满足形式而增加面板入口。
- ART-001 的资源交付合同、导入往返与 GUID／引用一致性属于一次性交付管线验证，保留
  现有验证场景、报告和证据，不迁入功能验证中心。
- 无论是否接入验证中心，每个完成的开发任务都必须向用户提供直观、可复核的验收结果：
  可视效果优先提供固定条件截图；适合用户在 Unity 中操作的结果必须给出查看入口、操作
  步骤和预期效果；不可视的底层结果提供测试、日志、报告或前后对比证据。
- 完成自动化检查不等于用户验收。交付时必须明确说明用户应看到什么，以及结果与任务
  验收标准的对应关系。

### 待决与授权边界

- 统一执行／结果契约和首批内置验证模块尚待后续讨论确认。
- 当前只归档阶段性共识；尚未授权创建对应 OpenSpec，也尚未授权开始实现。

## 2026-08-21：多窗口职能协作第一检查点

### 已确认

- 后续允许按可并行职能拆分长期工作窗口，初始角色包括制作人、主美（3D）、美术
  （2D）、客户端程序、后端程序、专项策划和测试；以后按实际负载增减TA、VFX、UI等
  专业窗口，不要求一开始把所有角色常驻化。
- 当前窗口固定承担制作人职能：读取用户维护的任务表，划分执行批次和并行组，检查
  前置依赖与资源冲突，组织决策收敛、OpenSpec阶段门、联合验收、归档和任务表人工
  更新建议；默认不直接承担大段专业实现。
- 各专业窗口保留自己的长期上下文和工具环境。3D主要使用Blender与ArtResource，
  2D使用图像模型及外部绘图工具，客户端和测试共享主工程Unity但错峰操作，专项策划
  可以独立处理文档、表格和规则设计。
- 同一Unity工程不得由两个窗口同时驱动。客户端与测试交接时必须退出PlayMode、等待
  编译／导入结束、保存必要场景并说明当前Unity端口和状态。
- 各窗口继续遵守“只提交、不推送”；任务表永久由用户维护，专业窗口和制作人窗口
  只能给出手动更新建议。
- 并行OpenSpec沿用相同批次序号和职能后缀，例如`03-parallel-program-*`、
  `03-parallel-art-*`和`03-parallel-design-*`；每份OpenSpec仍对应2～5个可共同决策、
  实现和验收的任务。

### 待继续讨论

- 制作人派发单的持久化位置、最小字段和生命周期。
- 专业窗口是在OpenSpec建立前接收探索任务，还是在OpenSpec骨架建立后接收实施任务。
- 专业窗口回传提交、证据、风险和未决问题的固定格式。
- 并行窗口发生文件所有权、Unity占用、依赖变化或设计冲突时的仲裁和暂停规则。

### 2026-08-21派发结构确认

- 用户确认采用“两阶段派发”：制作人先生成方案探索派发单，专业窗口完成具体方案讨论
  和决策摘要确认后再建立、维护OpenSpec，制作人负责跨职能复核与最终归档。
- 持久化入口固定为`Docs/Development/Dispatch/`：`README.md`保存稳定流程，
  `ActiveAssignments.md`由制作人独占维护当前协作阶段和资源占用，`Active/`保存尚未建立
  OpenSpec的独立派发单。
- OpenSpec创建后，派发单移动到`openspec/changes/<change-id>/dispatch.md`，随change
  一起归档，不再建立单独的派发历史库。
- 协作阶段固定为待领取、方案讨论、等待复核、可实施、实施中、待验收和暂停；这些
  状态不等于任务表状态，也不得成为任务表的替代权威。
- 派发单固定字段、回传模板、文件／目录锁粒度、Unity占用交接和冲突抢占规则继续讨论。

### 2026-08-21派发字段与占用规则确认

- 用户确认派发单固定包含身份、目标与范围、已确认输入、专业窗口启动检查、待讨论问题、
  修改权限与占用、交付回传、暂停升级八部分；正式模板位于
  `Docs/Development/Dispatch/DispatchTemplate.md`。
- 写权限采用业务目录锁为主，高冲突文件单独锁定；普通文件不逐项登记，也不以整个项目
  为默认锁粒度。
- Unity实例、Blender源文件和外部绘图工程采用工具独占锁。主工程8090由客户端和测试
  错峰使用；用户于2026-08-24固定美术工程为8091，DCC源文件也必须登记占用者与释放条件。
- 多窗口共享工作区时，Git索引和提交操作采用短期独占锁。专业窗口禁止`git add -A`和
  `git add .`，只显式暂存派发范围；没有Git索引锁时不得暂存、提交、变基、合并、切换
  分支或重写历史。
- `ActiveAssignments.md`继续由制作人窗口独占维护，专业窗口通过回传申请更新，避免
  协作总览本身成为并行写冲突点。
- 依赖变化、设计冲突、紧急缺陷的暂停、抢占和恢复协议仍待下一轮讨论。

### 2026-08-21暂停、抢占与恢复协议确认

- 用户确认暂停分为局部、批次和项目三级。局部暂停只影响一个派发单；批次暂停冻结依赖
  公共接口、资源合同、设计或阶段门的相关派发；项目级暂停停止全部写操作，仅用于工程
  不可用、数据损坏、框架污染、Git归属不明或不可逆迁移风险。
- 抢占严重度独立使用S0～S2：S0阻塞全项目并允许经批准抢占，S1由制作人调整批次顺序，
  S2回到原窗口排队修复；不得把任务表P0／P1直接解释为抢占等级。
- 暂停时立即释放Unity、Blender、外部工具和Git索引锁。目录内存在不可编译的未提交现场
  时，目录锁继续归原窗口；不允许其他窗口覆盖，也不在共享工作区随意使用全局stash。
- 只有修改可编译、引用完整且边界清楚时才建立检查点提交并释放目录锁。检查点不代表
  任务完成，不得据此更新任务表完成状态。
- 恢复必须重新核对阻塞、派发范围、任务依赖、OpenSpec、目录／工具锁、Unity健康、
  Git暂存区和最后有效恢复点，再由制作人更新协作状态。
- 正式协议位于`Docs/Development/Dispatch/PauseAndRecovery.md`；基础多窗口工作流至此
  已覆盖派发、方案门禁、锁、交付、暂停、抢占和恢复。

### 2026-08-21窗口与Agent路由检查点

- 用户确认窗口固定长期职能域，但不与单个全能Agent绑定。派发单分别指定方案、实施和
  协作职责：主美由`art-director`到`art-3d`，2D由`art-director`到对应2D／UI／字体
  实现职责，客户端由`client-lead`到`client-unity`／`tools-engineer`／`client-ta`，
  后端由`net-lead`到`net-backend`／`net-db`，测试固定为`qa-engineer`且默认只报告。
- 制作人窗口固定使用`producer`职责进行任务表只读核对、批次拆分、调度、复核、验收和
  归档，默认不承担常规专业实现。
- 现有Agent与SKILL矩阵没有玩法、系统、数值或动作策划职责。用户确认新增一个项目级
  `game-designer`，由派发单注明数值、动作、系统等专项方向，不预建多个常驻策划Agent。
- `game-designer`初始计划复用`grill-with-docs`、`xlsx`和`state-machine`；专业策划SKILL
  只在真实任务证明存在稳定缺口后再设计。
- 当前只确认总体方向，尚未授权创建Agent。文档／配置修改权限、设计变更门禁、跨职能
  权威、最终白名单和审计兼容方式继续讨论，完整决策摘要确认后再实施。

### 2026-08-24窗口能力与自动派发确认

- 用户确认长期工作窗口不限制可用SKILL，只按职能设置优先SKILL；Agent白名单仅约束
  真正显式委派的子Agent，不约束用户可见的长期职能窗口。
- 专项策划作为长期窗口职能，不新增`game-designer` Agent，也不修改框架SKILL矩阵；
  派发单按数值、动作或系统等专项方向限定上下文。
- 多窗口采用“首次注册、后续自动派发”：用户创建或明确授权创建长期职能窗口后，制作人
  保存本机窗口标识，自动发送派发单入口并监控完成／阻塞状态；正式事实仍以派发单、
  OpenSpec、提交与验收证据为准。
- 未经用户明确要求不自动创建新窗口；窗口未注册、失效、重名或项目不匹配时降级为手动
  派发，不把任务猜测发送到其他项目窗口。
- Unity Skills端口固定为：`stellar-frontier-auto-era`使用8090，`ArtResource`使用8091。
  固定端口不取消Unity实例独占锁，重要操作前仍须核验端口对应项目。

## 2026-08-24：GameData AI中间层纠偏第一检查点

### 已确认

- AI不得直接创建或修改任何GameData `.xlsx`；AI可编辑的作者输入必须是结构化JSON中间层。
- `DataTable`、`Config`和`Language`三类GameData都要具备统一的JSON导出、校验、反向生成、
  同步检查和正式生成流程，不能只让DataTable支持AI工作流。
- `.xlsx`继续作为正式数据源和人工维护入口；JSON是AI专用中间层，不与xlsx并列为同等权威。
- 人工修改xlsx后，必须由工具重新导出JSON再交给AI；AI修改JSON后，工具只有在确认xlsx自
  导出以来未变化时才能事务性回写。
- JSON和xlsx同时发生修改时必须硬失败并报告冲突，禁止按时间戳静默选择覆盖方向。
- 新数据允许由AI先创建合法JSON，再由工具首次生成xlsx；随后继续沿正式GameData生成链产生
  TXT、bytes、C#和验证报告。

### 待继续讨论

- 三类JSON与xlsx的镜像目录、业务分类和命名规则。
- 同步指纹、首次生成、备份／回滚、冲突处理和报告的具体门禁。
- DataTable代码生成Profile的Editor-only承载方式及程序集边界。
- 需要更新的OpenSpec条目和新增授权的精确`ScriptsBuiltin`文件；确认前保持第2段暂停。

### 2026-08-24目录、Profile与安全门禁确认

- 三类JSON与xlsx按相同业务相对路径镜像，当前项目基础数据使用`Foundation/`业务类别；
  GameData输入不得机械增加`AutoEra/`目录层。
- DataTable生成Profile使用独立Editor-only `GameData/AIData/GenerationProfiles.json`承载，
  不写入运行时`AppConfigs`，不通过Hotfix初始化代码反向引用`Builtin.Editor`，也不新增asmdef。
- Profile只保存领域无关的源相对路径、代码输出根和C# namespace；产品值存在项目JSON中，
  框架生成器不得硬编码`AutoEra`。
- 同步基线使用规范化单元格逻辑内容指纹，不使用容易受Office容器元数据影响的原始xlsx哈希。
- Reverse发现当前xlsx指纹与JSON记录的导出基线不一致时必须硬失败；第一版不提供AI可调用的
  强制覆盖参数，只能重新从xlsx导出JSON后再编辑。
- 新表仅在目标xlsx不存在时允许由合法JSON首次创建；意外同名文件、路径越界、Schema错误、
  重复键／ID或非法值均在正式写入前失败。
- 工具必须先在临时位置完成验证，再事务性替换正式xlsx和生成物；失败时从`Temp`备份恢复，
  并输出包含指纹、变更行／单元格、错误和回滚结果的结构化报告。
- 验收必须覆盖三类数据往返一致、人工／AI并发冲突、新表创建、路径越界、注入失败回滚和
  Core数据默认行为不变。

四轮关键决策至此完成，可以修订既有
`b02-parallel-program-p0003-p0006-runtime-foundations` OpenSpec；新增框架核心文件仍须用户对
精确路径另行授权后才能实施。

## 2026-08-24：ART-004／ART-005参数复用与表面痕迹检查点

### 已确认

- 机器关节按轮组、旋转、伸缩和效应器接口建立可复用参数模板，模板提供默认速度、加速度、
  回弹、限制与安全规则；具体资产只覆盖差异。
- 工坊和处理站使用痕迹保持克制，磨损、积尘、热痕和维修标记优先集中在入口踏面、平台边缘、
  功能接口、散热口、维护面板和作业端，大面积表面保持清朗。
- 痕迹按功能位置和使用逻辑分布。同类模块共享痕迹规则并允许实例轻微变化，不采用均匀随机
  脏污，也不要求每栋建筑完全手绘。

### 边界

- 本检查点只冻结后续方案讨论输入，不授权美术试制、Unity／DCC操作、文件修改或OpenSpec创建。
- 关节模板数据结构、资产覆盖层级、痕迹强度范围与代表性样张仍待后续讨论确认。

## 2026-08-24：ART-006轻量面板与告警交互检查点

### 已确认

- 右侧轻量面板采用对象优先的信息结构。选中对象后按名称／状态、当前任务与阻塞原因、
  `2～3`个快捷操作、中枢详情入口的固定顺序展示。
- 未选中对象时，右侧轻量面板回退为告警和最近任务摘要，不保留空对象详情框架。
- 点击告警优先聚焦对应现场对象，再在右侧面板展示原因、影响和可执行操作；只有需要深入分析
  时才进入中枢详情。
- 该交互继续维持极简HUD、现场优先和按需展开基线，普通告警不直接把用户带离现场视图。

### 边界

- 本检查点只冻结后续UI方案输入，不授权UI制作、Unity操作、文件修改或OpenSpec创建。
- 无对应现场对象的告警降级路径、快捷操作安全边界和具体布局样张仍待后续讨论确认。

## 2026-08-24：ART-006中枢导航与总览层级检查点

### 已确认

- 中枢全页采用任务导向主导航，顺序为总览、待处理／任务、对象与系统、规则／自动化、统计。
- 中枢“总览”页优先呈现待处理事项：严重告警与阻塞置顶，当前任务／队列居中，资源／产能摘要
  置底；总览摘要用于导向决策，不承担完整报表职责。
- 完整信息链固定为HUD告警、现场对象聚焦、对象优先右侧面板、中枢详情，避免普通告警直接跳转
  全页，也避免复杂管理信息长期占据HUD。

### 边界

- 本检查点只冻结后续UI方案输入，不授权UI制作、Unity操作、文件修改或OpenSpec创建。
- 各导航项二级页面、总览卡片数量上限和无待处理事项时的空状态仍待后续讨论确认。

## 2026-08-24：ART-005／ART-006规则编辑与程序化表现检查点

### 已确认

- 规则／自动化首阶段采用规则卡片和表单，以“触发条件→判定→执行动作”的可读卡片、列表与
  弹窗为主；节点／连线只作为未来扩展语言，不是首阶段前置功能。
- 规则保存或启用前展示影响预览，明确触发时机、影响对象和执行动作，并区分“仅保存草稿”与
  “立即启用”。
- 首个轮式载体采用四轮独立地面接触采样、悬挂贴地和四点平滑车体姿态拟合；轮子半径决定
  台阶高度阈值，连续坡度另用可配置规则处理。
- 可动资产统一使用六层程序化表现合同：可动结构、基础运动原语、环境感知、状态阶段、表现
  编排、细节反馈。美术定义结构、限制与表现参数，程序负责状态、求解和运行时控制。
- 该合同复用于移动机器、建筑机构和资源点；未来敌人只保留可扩展契约，不纳入当前第一版制作。

### 边界

- 本检查点只冻结后续方案输入，不授权美术／UI制作、Unity／DCC操作、文件修改或OpenSpec创建。
- 规则卡片字段密度、预览弹窗布局、坡度参数边界和六层合同数据接口仍待后续讨论确认。

## 2026-08-24：ART-005／ART-006纵向样机与状态反馈检查点

### 已确认

- 统一程序化表现合同的纵向样机范围为一台轮式机器和两个轻量建筑机构，仅用于技术与表现验证，
  不作为正式量产资源。
- 建筑机构采用滑动门和传送带：滑动门验证线性开闭、占用／告警和中断；传送带验证持续循环、
  进度／效率驱动与货物节奏；轮式机器机械臂继续覆盖旋转和伸缩。
- UI图标采用线性工程图标与状态填充。默认状态克制，仅在激活、告警、资源焦点和关键操作时
  使用填充与工程强调色；关键图标由文字标签或说明补充语义。
- 状态分为严重、需处理、正常／未激活三级。HUD只常驻严重项，需处理项进入队列，普通状态留在
  对象面板；状态同时通过颜色、形状／图标与文字冗余表达。

### 边界

- 本检查点只冻结后续方案输入，不授权美术／UI制作、Unity／DCC操作、文件修改或OpenSpec创建。
- 纵向样机验收动作、货物占位表现、图标笔画规范和三级状态视觉样张仍待后续讨论确认。

## 2026-08-24：ART-006统计页与HUD安全区检查点

### 已确认

- 统计页采用异常与趋势优先，首屏展示偏离目标的指标、短周期趋势和原因入口；完整明细按需展开。
- `1920×1080` HUD采用四角分区：左上资源摘要，右上时间／速度与中枢入口，左下严重告警，
  右下当前选中对象及快捷操作；中央现场区域保持清晰。
- 四角布局继续遵守极简HUD、对象优先轻量面板和任务导向中枢的信息链，复杂统计不常驻HUD。

### 边界

- 本检查点只冻结后续UI方案输入，不授权UI制作、Unity操作、文件修改或OpenSpec创建。
- 安全边距、缩放策略、超宽屏／低分辨率适配和统计图表样张仍待后续讨论确认。

## 2026-08-24：ART-005地形门槛与作业稳定检查点

### 已确认

- 首个轮式机器连续坡度上限为`35°`；台阶高度以轮子半径为阈值，两者独立判定并可按机器类型
  分别配置。
- 作业稳定采用锁轮与可收放稳定支撑：对位后锁轮、放下两侧或后方支撑、展开机械臂；收回时
  按相反顺序执行。
- 可通行性由逻辑层裁决；表现层只负责允许范围内的接触采样、悬挂贴地和姿态拟合，不得强行
  贴地越过逻辑层判定不可通行的障碍。

### 边界

- 本检查点只冻结后续方案输入，不授权美术制作、Unity／DCC操作、文件修改或OpenSpec创建。
- 稳定支撑数量／位置、地面法线过滤和失败／中断时的安全收回表现仍待后续讨论确认。

## 2026-08-24：ART-005／ART-006节点语言与接触IK检查点

### 已确认

- 未来节点编辑器采用左进右出的预留语言：条件左、判定中、动作右，使用稳定端口与单向连线；
  节点沿用暖灰表面和深蓝灰结构线。首阶段仍采用规则卡片与表单，不以前置节点编辑器为门槛。
- 常规HUD／侧栏使用约九成不透明的半实色面板；规则确认、严重告警等模态弹窗才轻度压暗现场
  背景，不采用全局毛玻璃或全实色大面积遮挡。
- 稳定支撑分别进行独立地面探测，在最大行程内接触地面，脚垫有限贴合法线，车体只做平滑补偿，
  不进行完整受力仿真。
- 机械臂目标包含位置、表面法线和推荐接近方向；IK在关节限制内对准采掘、装卸或维修目标，
  首阶段不采用完整碰撞求解或力反馈。

### 边界

- 本检查点只冻结后续方案输入，不授权美术／UI制作、Unity／DCC操作、文件修改或OpenSpec创建。
- 节点尺寸／端口规范、模态压暗强度、支撑行程失败处理和IK不可达降级表现仍待后续讨论确认。

## 2026-08-24：ART-005空间约束与重新对位检查点

### 已确认

- 可动资产提供`KeepOut`净空区域和`WorkPoint_*`／`Socket_*`稳定锚点，用于作业对位、连接和
  避免明显视觉穿插；逻辑碰撞、导航和可通行性权威仍属于主工程。
- 机械臂目标不可达或净空不足时进入“需要重新对位”：机械臂保持或回到安全姿态，底盘移动到
  合适作业位置后再对位。
- 不允许强行伸到关节极限，也不允许IK或表现层忽略净空及逻辑约束继续作业。

### 边界

- 本检查点只冻结后续方案输入，不授权美术制作、Unity／DCC操作、文件修改或OpenSpec创建。
- `KeepOut`形状表达、锚点命名校验、候选重定位点来源和重新对位失败反馈仍待后续讨论确认。

## 2026-08-24：ART-005参数化门族与传送带检查点

### 已确认

- 滑动门采用参数化门族，覆盖单／双扇、`2 m`／`4 m`／`8 m`模块宽度、行程、速度、停靠点、
  状态灯和安全感应区；建筑实例只选择参数、外壳与细节。
- 传送带通过皮带／链板循环、滚轮转动、`LoadPoint`／`UnloadPoint`锚点、速度和效率参数共同
  驱动作业节奏。
- 传送带既不采用仅滚动贴图的最低表现，也不进行完整物理货物模拟；逻辑状态仍由主工程拥有。

### 边界

- 本检查点只冻结后续方案输入，不授权美术制作、Unity／DCC操作、文件修改或OpenSpec创建。
- 门族参数默认值、安全感应区反馈、传送带货物可视数量和效率映射曲线仍待后续讨论确认。

## 2026-08-24：ART-006 GF UI强制工作流检查点

### 已确认

- `Docs/Development/GF-UI-Standards/README.md`及`01`～`08`纳入ART-006后续方案、OpenSpec、样张、
  UI美术交付、Prefab／代码接入和验收的强制工作流，所有后续派发必须明确引用。
- 每个独立页面必须先定义目的、入口／出口、数据、操作、异常和完整状态，并具备状态表、
  `prefab-layout.md`、视觉稿和验收证据；layout确认前不得创建正式Prefab。
- UI美术交付必须包含token、至少五级字号、唯一TMP Font Asset、可编辑源、切片／导入／九宫格
  说明、资源清单和验收截图；UIForm、输入和生命周期按GF边界接入。
- `1920×1080`保留为主视觉评审基准，但不是唯一适配验收；正式验收至少覆盖`16:9`、`16:10`、
  `21:9`、鼠标／键盘／手柄焦点、可访问性、性能和规范`07`清单。

### 边界

- 本检查点更新后续工作流，不授权当前UI制作、Unity操作、Prefab／代码修改或OpenSpec创建。
- 项目UI接入模板中的具体字体、CanvasScaler、安全边距、目录、UIGroup与输入映射仍需在实施前填写。

## 2026-08-24：ART-005／ART-006安全反馈与代表样张检查点

### 已确认

- 滑动门临时占用时减速、暂停并回弹／重新打开，显示黄色提示；持续异常或故障才转红色。
- 传送带下游堵塞时显示黄色等待并受控减速，链板、滚轮和货物保留位置，恢复后平滑继续。
- 轮子转动按实际移动距离与接地状态驱动，真实移动才转、停止即停；只有逻辑明确的打滑状态
  允许接地空转。
- 高保真UI样张使用三张代表界面：现场（HUD＋对象优先面板＋严重告警）、中枢（任务导向总览）、
  规则（规则卡片＋影响预览确认）。
- 严重告警使用左下单条行动告警卡，只展开最高优先级的图标、短原因、影响对象和主操作，
  其他项进入计数队列。
- 样张以`1920×1080`为主设计基准，但必须同时按GF UI规范验收`16:9`、`16:10`、`21:9`，
  1080p不得作为唯一适配证据。

### 边界

- 本检查点只冻结后续方案和样张输入，不授权美术／UI制作、Unity／DCC操作或OpenSpec创建。
- 门占用回弹参数、传送带恢复曲线、打滑状态合同、样张状态表和告警队列展开方式仍待确认。

## 2026-08-24：ART-006告警队列与异步反馈检查点

### 已确认

- “需处理”级告警使用折叠计数队列，点击后按影响与时间展开轻量列表，不常驻挤占现场。
- 异步操作使用控件原位反馈：按钮加载并防重复点击；成功在触发对象／规则卡原位显示短暂结果；
  失败原位说明原因和可恢复动作。
- 常规异步反馈不以全局Toast或频繁模态为主，只有跨页面、系统级或阻断性事件才升级反馈层级。
- 页面状态表和实现验收必须覆盖加载、成功、失败、取消、超时、关闭与晚到回调，遵守
  GF UI Standards的异步收口规则。

### 边界

- 本检查点只冻结后续UI方案输入，不授权UI制作、Unity操作、文件修改或OpenSpec创建。
- 队列最大可见项、同对象告警合并、成功反馈持续时间和失败重试入口仍待后续讨论确认。

## 2026-08-24：ART-006焦点与关闭层级检查点

### 已确认

- 键盘／手柄焦点采用橙色外框与轻微缩放；hover只作补充，焦点和功能理解不依赖hover。
- Esc／取消只关闭最上层可关闭界面，并恢复触发控件焦点，不越级关闭底层页面或退出游戏。
- 返回层级为规则确认弹窗、右侧轻量面板、中枢全页；顶层消费输入后，底层不得重复响应。
- 页面状态表、layout、Prefab和验收证据必须明确默认焦点、导航顺序、关闭目标及焦点恢复目标。

### 边界

- 本检查点只冻结后续UI方案输入，不授权UI制作、Unity操作、文件修改或OpenSpec创建。
- 焦点缩放幅度、不可用控件跳过、动态列表焦点恢复和系统级不可关闭错误仍待后续讨论确认。

## 2026-08-24：ART-005底盘转向检查点

### 已确认

- 首个轮式机器采用四轮转向，每轮保留独立转向轴、悬挂行程、接地点和转向限制。
- 支持前后轮反向转向实现小半径转弯，并支持四轮同向蟹行，在保持车头方向时完成斜向／横向
  低速微调。
- 第一版不加入高速同向转向；所有转向模式继续受四轮独立接地、`35°`坡度、轮半径台阶阈值、
  逻辑通行裁决和重新对位流程约束。

### 边界

- 本检查点只冻结后续方案输入，不授权美术制作、Unity／DCC操作、文件修改或OpenSpec创建。
- 各模式转角上限、切换速度门槛、轮速差分和蟹行车体表现仍待后续讨论确认。

## 2026-08-24：ART-005微调与效应器装卸检查点

### 已确认

- 大幅移动必须先安全收臂；只有低速蟹行微调可以保持抬起的对位姿态，且效应器必须离开目标、
  位于`KeepOut`净空内，机械臂只做有限补偿。
- 净空、关节限制或稳定条件不满足时停止微调并进入安全姿态／重新对位，不允许移动中强行
  保持目标接触。
- 可替换效应器使用参数化装卸序列：接口对位、锁扣解锁、旧件脱离、新件接入、锁定和状态确认，
  由程序化关节、线性／旋转原语与事件触发完成。
- 第一版不采用即时替换或自由装配模拟，装卸序列必须可中断并安全收口。

### 边界

- 本检查点只冻结后续方案输入，不授权美术制作、Unity／DCC操作、文件修改或OpenSpec创建。
- 蟹行微调速度／距离上限、机械臂补偿范围、锁扣反馈和装卸中断恢复点仍待后续讨论确认。
