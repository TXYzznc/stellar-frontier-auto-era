# B10 客户端覆盖与未覆盖边界

日期：2026-09-10。此表是实现／验证记录，不是用户最终视觉验收，也不代替整批G0/G1裁决。

| 范围 | 当前实现与证据 | 不应推导的结论 |
| --- | --- | --- |
| 基础复用 | client-baseline与b02 segment-22；既有ID、注册表、时间、上下文复用，补GF实体与启动流 | 不代表P0-011完整第一版数据骨架已完成 |
| 三类对象 | 独立InitialRegion、8个GF实体、世界注册/释放与HUD计数 | 代理不代表正式建筑模型或完整资源生产 |
| 选择 | 可替换输入源、选择层Trigger、失效清空、UI阻挡、悬停/选中轮廓 | 自动输入源测试不称作真实硬件人工验收 |
| HUD | 机器/建筑/资源摘要，计数、作业通道与只读状态；1920×1080截图交2D复核 | 测试临时状态与额外纯数据机器不是正式实体交付 |
| 占地 | 旋转矩形、0.5米/15度吸附、提交时重验、确认单次消费、取消 | 不包括施工扣费、实际建筑创建与完整施工流程 |
| 作业 | 真实服务优先级/同级FIFO、释放移交、失效、双通道、HUD投影 | 不新增手动作业按钮；不代表算法触发、导航排队或物流结算 |
| 区域代理 | 80×80固定区域，资源点可区分；矿脉可通行但挡施工 | 不构成矿产量、树木生长或资源点完整权威 |
| 启动 | 正式Launch→菜单→世界两轮、8实体释放、失败菜单恢复；双击断言、真实GF取消与重新加载通过 | 不代表框架重启或所有加载故障组合均已覆盖 |

## 验证记录

逐项Unity job、XML及截图见client-baseline.md与QA独立矩阵。取消回调首轮734b976b为2/3：
首轮空引用发生在Load之前，不能声称取消逻辑通过。v2/v3仍在iterator中空引用，早期仅按行号推断配置来源的问题未获证实。
v4将带回调闭包的验证独立为EnterPlayMode域重载后创建的协程，保持产品SceneFlow不变；
权威XML结束时间2026-09-10 08:44:48Z记录3/3通过，取消例6.744335秒。
output含v4标记、Load前/后及old callbacks=0、fresh callbacks=1、owned scene released完整阶段记录。
这支持先前失败来自测试协程跨域重载状态，而非产品取消逻辑；不把修正前结果混入通过数。

2026-09-10静态门禁：OpenSpec严格校验通过、项目边界通过、显式产品纯度通过、纯度审计单元测试14/14。
未触碰Git索引、未自动请求提交、未直接编辑xlsx。

## 用户查看入口

正常入口为已保存的Assets/Game/Scene/Launch.unity，运行后从功能菜单进入初始区域。
WASD、右键拖动、滚轮控制镜头；左键选择、空白取消，F/双击聚焦；管理UI阻挡世界输入。
Editor Runtime Evidence菜单仅为开发取证夹具，不是玩家作业入口；停止PlayMode后临时改动不保留。

## 仍需分别保留

- QA真实输入矩阵；不得以纯数据服务测试替代硬件操作记录。
- 用户最终视觉判定，包括代理、轮廓和极端文本；2D复核不替代用户决定。
- 静态美术生产由对应角色承接，不在客户端任务中勾成完成。
- 整批G0/P0剩余项沿用b02对账，不借本批最小启动配置自动归零。

## QA 收口复核

QA最终回传 adb08994 / XML 3/3、Console Error=0 Warning=0、8090释放，未发现新增已定位且未修复的产品缺陷。
本职2.1–2.6实现项据此更新；1.2仍保留未勾选，避免把已核对但尚未整体补齐的P0项目算成完成。
剩余证据：独立物理硬件输入链、极端长名称+长状态+只读组合、瞬时加载截图、hover/焦点视觉差异；
历史Physics序列化新增默认字段缺少升级前有效值，不声称等价。该差异已在既有派发授权中保留。
客户端交付与用户视觉、整批G1、其他美术职责分别处理；不自动Git。
