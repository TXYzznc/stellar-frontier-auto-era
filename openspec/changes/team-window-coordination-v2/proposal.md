# 提案：队列驱动的可替换窗口协同

## 增量范围：可靠通知与调度唤醒

在原有共享状态和不抢占规则上，增加可确认、可重试的共享事件队列。调度窗口作为低频唤醒器：目标窗口 Active 时只记录事件；目标窗口空闲时发送最小唤醒信号；仅决策、授权、资源冲突和安全风险立即升级制作人。

事件队列是通知传输层，派发单、OpenSpec、代码和验收证据仍是事实来源。该增量不改变产品范围、Unity资源权限或Git手动触发规则。
