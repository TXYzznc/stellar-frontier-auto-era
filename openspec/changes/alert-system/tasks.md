# 任务

## 1. 警报目录

- [x] 1.1 `AlertSeverity`（提醒／警告／严重，数值可比大小）、`AlertState`（活跃／已恢复）、
      `AlertKind`（缺电停机／电量耗尽／燃料耗尽／机器完全损坏）。
- [x] 1.2 `AlertCatalog`：每一类的等级、来源、目标、触发原因、影响与**真实恢复条件**。
      测试钉住「每一类都必须写得出真实恢复条件」——那是做成警报的准入条件。
- [x] 1.3 只做四类能直接读出真值的问题；队列溢出与算法异常**明确不做**（缺判据），
      理由写在代码与变更记录里。

## 2. 警报账本

- [x] 2.1 `AutoEraAlertService`：合并键＝（种类，来源），**一行一问题**。
- [x] 2.2 `Raise`：新建或重新活跃；条件持续存在期间**零写入**（不改次数、不改时间、不通知）。
      `次数` 是发生过的段数，不是轮询次数。
- [x] 2.3 `Resolve`：真实条件不成立 → 转历史并记恢复时间；没有活跃条目时无副作用。
- [x] 2.4 `MarkRead`：只改阅读状态；重新发生时重新变未读。
- [x] 2.5 排序由账本给：先活跃（等级降序、最近发生在前），再历史（恢复时间倒序）。
- [x] 2.6 `HighestActiveSeverity` / `ActiveCount` / `UnreadCount`；`CopyInto` 不清空目标列表。

## 3. 生产者

- [x] 3.1 `RegionAlertMonitor`：每节拍读真值、只在跨越时让账本变化；不订阅事件、不缓存判断。
- [x] 3.2 缺电停机的判据用 `MachineEnergyConsumer.IsStoppedByShortage`，不用 `SupplyAvailable`。
- [x] 3.3 机器完全损坏（完整度 ≤ 0）；燃料设施无可燃生物质；储能设施电量 ≤ 0（且容量 > 0）。
- [x] 3.4 `InitialRegionScene`：结算之后推进警报；真实跨越写进事件日志（`EventDomain.Alert`）；
      区域释放时清空账本与监视器。
- [x] 3.5 警报账本随会话交给界面（`AutoEraUiSession.RegionAlerts`，`ForWorld` 第 7 个可选参数）。

## 4. 读模型

- [x] 4.1 `AlertDomainSnapshot` / `UiAlertRow`：行给出等级、种类、来源名、状态、次数、首次／最近／恢复时间。
- [x] 4.2 `AlertReadModels.Create`：缺会话／不在世界里／区域未就绪各有可辨原因；
      账本活着但没有警报是 `Ready` + 空列表（「当前没有问题」不是「不可用」）。
- [x] 4.3 写入口只有 `MarkRead`；接口里没有 Resolve——界面既不能制造恢复，也不能清除问题。
- [x] 4.4 `AlertDetails.Build`：等级／状态／来源／目标／触发原因／影响／真实恢复条件／
      首次／最近／恢复时间／次数／阅读状态。
- [x] 4.5 `AutoEraUiFormat.AlertSeverity` 给出「提醒／警告／严重」。

## 5. 验收（本批）

- [x] 5.1 编译 0 错。
- [x] 5.2 门1 契约自检全绿（33/33）；`run_project_checks.py` 5/5。
- [x] 5.3 `AutoEraAlertServiceEditModeTests`（**9/9**）：新建即活跃未读；持续存在零写入；
      恢复转历史；再次发生重新活跃且次数 +1、首次时间保留；标记已读不解决问题；
      排序（活跃优先、等级降序、最近在前）；`CopyInto` 不清空；`Clear`；
      目录每一类都有等级与真实恢复条件。
- [x] 5.4 `RegionAlertMonitorEditModeTests`（**9/9**）：缺电停机报警、补燃料后自己恢复（两条都转历史）；
      从未接电的机器不算事故；损坏报警且修复后恢复；储能耗尽与充电；无储能区域不报耗尽；
      燃料耗尽与补充；环境能源不报燃料；库中机器不监视；没有电网时机器损坏照样报警。
- [x] 5.5 `AlertReadModelEditModeTests`（**9/9**）：三种不可用原因；空账本是 Ready；
      行带来源名与计数；选中给详情字段（含真实恢复条件）；标记已读只改阅读状态；
      账本变化重建快照并通知；释放后不再回调。
- [x] 5.6 回归：EditMode 全类、PlayMode 14 套全绿；门1 33/33 持续绿。

## 6. 界面接线（下一批，未开始）

- [ ] 6.1 `AlertForm` 页面：列表（活跃／已恢复筛选）、详情、标记已读、定位、相关详情。
      绑定与接线要走契约（`EXTRA_BINDINGS`）＋「按契约刷新所有页面绑定」＋门1。
- [ ] 6.2 HUD 警报摘要模块：最高等级 + 来源 + 主要原因 + 活跃总数；「查看全部警报」打开筛选活跃的列表。
- [ ] 6.3 中枢：总览「最近活跃警报」、待办「活跃警报」分类、统计的记录栏补警报历史入口。
- [ ] 6.4 `Btn_AlertsLocate`（有效对象聚焦）依赖尚未接入的跨对象定位通道——与 `Btn_HubEnergyLocate` 同一批。
- [ ] 6.5 PlayMode：真实区域里制造一次缺电 → HUD 摘要出现 → 打开警报列表 → 标记已读 → 定位／相关详情。

## 7. 明确不做

- 队列溢出、算法异常的警报（需要先有各自的判据）。
- 任何「清除故障」「重置真实状态」的入口。
- 警报的存档落盘（与能源事件一起留给存档批次）。
