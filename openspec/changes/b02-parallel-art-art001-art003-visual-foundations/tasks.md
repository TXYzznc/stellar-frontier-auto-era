## 1. ART-001 项目与合同基线

- [x] 1.1 记录主工程与 `ArtResource` 的 Unity、URP、渲染设置、必要包和当前 Console 健康状态，确认两个项目分别可通过当前 Unity Skills 连接访问。
- [x] 1.2 在 `ArtResource` 建立并记录 DCC 可编辑源、Unity 制作资产、LookDev、导出暂存和交付证据的职责分区；不得把本机绝对路径写入可复用工具逻辑。
- [x] 1.3 编写 ART-001 交付合同，覆盖1单位＝1米、坐标／前向、原点／Pivot、局部轴、命名、模型与贴图导入、材质、Prefab、Socket、作业点、VFX点、状态灯点、关节元数据、版本和依赖声明。
- [x] 1.4 建立可逐项检查的导出前与导入后验收清单，并明确美术源修改必须回流 `ArtResource`、任务表永久保持 AI 只读。
- [x] 1.5 制作一个最小环境／建筑样例和一个可拆分机器 Blockout，覆盖材质、锚点、稳定关节ID、关节范围、默认姿态与安全收回姿态。
- [x] 1.6 验证代表性机器的可动部件在声明范围内不会移动逻辑根、破坏主碰撞边界或遮挡必要 Socket 与状态反馈位置。
- [x] 1.7 为最小交付包建立版本与内容清单，排除 DCC 源、缓存、无关项目资产、GF_X、AutoEra 代码、Adapter 和动作工具源码。
- [x] 1.8 从 `ArtResource` 导出保留 `.meta`／GUID 的最小 `.unitypackage`，在主工程目标位置完成首次导入并检查比例、朝向、材质、锚点、GUID、依赖与Prefab引用。
- [x] 1.9 重复导入同一包版本，确认不产生重复身份、不丢失既有绑定，并记录失败项、修复和最终往返证据。

## 2. ART-002 视觉方向探索

- [x] 2.1 建立统一 LookDev 场景，固定相机、尺度参照、代表对象、灯光、后处理、质量设置和远／中／近评审视角。
- [x] 2.2 建立参考池与来源记录，并为三组候选分别编写目标、参考、约束和输出范围；三组使用同一代表对象清单。
- [x] 2.3 制作候选一的环境、机器、建筑、色彩、材质与灯光最小完整样片。
- [x] 2.4 制作候选二的环境、机器、建筑、色彩、材质与灯光最小完整样片。
- [x] 2.5 制作候选三的环境、机器、建筑、色彩、材质与灯光最小完整样片。
- [x] 2.6 在每组候选中加入未激活、已激活、警告和损坏状态，以及机器前向、传感器、效应器、资源点、通行空间和作业区域的可读性检查。
- [x] 2.7 使用固定视角和相同输出条件生成三组对比证据，避免用专属特写或不一致光照掩盖俯视游戏镜头问题。
- [x] 2.8 完成统一评审矩阵，比较风格契合、俯视可读、功能可读、状态色冲突、制作成本、模块复用、程序动作适配和性能风险。
- [x] 2.9 向用户提交候选对比与推荐；取得明确确认后，只冻结一套正式视觉基线，并记录选择或融合来源、Do／Don't、色彩、材质、轮廓、灯光和细节层级。

## 3. ART-003 场景路线样片

- [x] 3.1 定义同一代表性测试区的固定玩法骨架，包含基地／工程设施、自然环境、通行路线、一个权威资源点、作业与装卸点、建筑占地、导航空间和生成排除体积。
- [x] 3.2 使用冻结视觉基线，对 Unity Terrain、模块化拼装、手工布景和混合方式完成同范围轻量试制，记录各自资产需求和制作时间。
- [x] 3.3 实现仅在 Editor 使用的最小视觉环境生成原型，使其只在授权区域布置地表过渡、植被、碎石和非交互装饰，不创建逐实例玩法身份。
- [x] 3.4 验证生成器尊重道路、导航走廊、作业点、装卸点、选择范围、建筑占地和其他玩法净空。
- [x] 3.5 验证生成结果可以保存、重开、人工修改和按明确区域选择性重生成；未明确重生成的人工修改不得被覆盖。
- [x] 3.6 完成混合路线代表性样片，覆盖地表过渡、植被群落、碎石、资源点视觉群落以及至少两种代表性照明条件。
- [x] 3.7 在声明的游戏相机、质量设置和硬件环境下记录对象数量、Renderer／材质情况、批处理或实例化条件、CPU／GPU帧时间和明显瓶颈。
- [x] 3.8 完成四种方式的对照报告，确认混合路线中 Terrain、模块化、手工布景和生成工具各自职责；若触发重评条件，停止深化并回到用户决策。
- [ ] 3.9 向用户提交场景样片、资产清单、性能证据、已知限制和建议的后续密度／材质／LOD决策，取得 ART-003 方向确认。

## 4. 联合验收与移交

- [ ] 4.1 按三份 capability spec 逐项执行验收，确认没有把运行时随机地图、动物动画、Motion Core／Editor、AutoEra Adapter、正式批量资源或框架核心修改带入本变更。
- [ ] 4.2 整理 ART-001～ART-003 的交付合同、往返证据、候选评审、视觉基线、场景样片、资产清单、Profile 记录、风险和未决预算，形成后续 Art Bible 与程序动作任务可直接引用的移交包。
- [ ] 4.3 在主工程运行 OpenSpec 严格校验、项目边界审计、框架纯度审计和相关 Unity Console／引用检查，修复本变更引入的问题。
- [ ] 4.4 报告 `ART-001`、`ART-002`、`ART-003` 的建议状态、实际工时和依赖观察，但不修改 `第一版开发任务表.xlsx`。
- [ ] 4.5 更新增量归档、设计状态与相关视觉文档，记录已确认结果、明确排除项、样片后预算和任何需要后续 OpenSpec 处理的事项。
