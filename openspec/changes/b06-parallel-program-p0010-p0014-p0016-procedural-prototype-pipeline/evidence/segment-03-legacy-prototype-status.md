# 第 3 段：旧样机与演示依赖状态

日期：2026-09-02

## 研究输入登记

- `b03-parallel-art-art004-art005-production-rnd` 中的 G06、ART-004／005 白模、运行时预览、截图、视频、Pivot 合同和样机算法仅可用于识别风险、比对术语和设计输入。
- 它们不是本 change 的层级、尺寸、轴、锚点、接口或运行时 API 权威，不能替代新 `FunctionalRigContract`、自动结构验证或动作回归。
- 用户已判定未经过前置出图门的旧 3D 模型不可作为正式交付；本段不复用其模型、Prefab、场景或视觉几何。

## 七段动画演示状态

- `b03-parallel-program-art004-art005-animation-demo` 的 proposal、design 与 tasks 均记录为“暂停”。
- 恢复条件仍为：替代机器、工坊、门、传送带和效应器完成前置出图、建模路线审批及模型验收；本 change 的纵向切片是新增功能结构前置条件，不会提前启动该演示。
- 未修改暂停中的动画演示 change、ArtResource、Unity 场景、Prefab、任务表或 `.xlsx`。
