## 0. 变更门与依赖校正

- [x] 0.1 对照任务表只读核验 `P0-010`、`P0-014`、`P0-015`、`P0-016` 的状态和依赖，记录用户手动调整建议且不修改xlsx
- [x] 0.2 将 `b03-parallel-art-art004-art005-production-rnd` 中与“模型先行”冲突的生产顺序同步为程序功能原型先行，保留既有失败证据与美术视觉决策
- [x] 0.3 核验现有Hotfix、HybridCLR、FSR和asmdef引用边界，确定Motion Core／Editor／AutoEra Adapter的物理程序集布局且不修改 `Assets/Game/ScriptsBuiltin/`
- [x] 0.4 登记现有G06与ART-004／005样机为研究输入而非新合同权威，并保持七段动画演示变更暂停

## 1. P0-010 合同级结构原型基础

- [x] 1.1 定义确定性 `FunctionalRigContract` JSON schema、版本、稳定ID、坐标、关节、锚点、净空、碰撞包络、视觉槽和兼容元数据
- [x] 1.2 实现合同读写、规范化排序、内容指纹和结构校验，覆盖缺失字段、重复ID、非法路径、非法限制和不兼容版本
- [x] 1.3 建立逻辑根、Rig根、关节、视觉槽和权威碰撞边界的Prefab分层模板，验证视觉关节不移动逻辑根
- [x] 1.4 实现幂等原型Builder，用立方体／圆柱等基础几何生成或更新合同层级且不重复对象
- [x] 1.5 实现无支撑／游离可见件、错误父级、Transform漂移、缺失锚点和视觉槽越界的自动检查
- [ ] 1.6 完成结构原型EditMode测试并把合同检查入口接入固定测试面板

## 2. P0-014 程序化动作核心与配置模型

- [ ] 2.1 实现稳定关节ID、局部轴、范围、绑定姿态、安全姿态和通道声明的数据模型及 `MotionRig` 绑定
- [ ] 2.2 实现带版本、稳定节点ID、强类型参数、显式节点表与连接表的 `MotionGraphAsset`
- [ ] 2.3 实现旋转、伸缩／平移、瞄准、开合、持续旋转、往复和等待基础运动原语
- [ ] 2.4 实现顺序、并行、循环、条件等待和有限分支组合，并拒绝任意脚本节点与场景业务引用
- [ ] 2.5 实现单Rig集中式 `MotionExecutor`、准备／运行／完成／取消／恢复生命周期和关节通道所有权
- [ ] 2.6 实现保持、收回、归零和立即停止中断策略，确保从当前实测姿态平滑接续且无累积漂移
- [ ] 2.7 定义强类型参数上下文、目标Pose／环境输入窄接口和AutoEra Adapter边界，验证Adapter不直接操作关节或决定玩法结果
- [ ] 2.8 实现近／中／远／不可见表现更新等级，并验证降频或停更不改变玩法与碰撞权威
- [ ] 2.9 完成固定步进、不同帧分片、通道冲突、中断恢复、重复播放和运行热路径GC的EditMode／性能测试

## 3. P0-015 动作预览、校验与工具包

- [ ] 3.1 实现MotionRig、MotionGraph、参数上下文、阶段曲线和中断策略的Inspector配置与错误定位
- [ ] 3.2 实现EditMode／PlayMode预览、关节范围与锚点Gizmo、正例／负例参数注入及一键恢复绑定态
- [ ] 3.3 实现合同版本、Rig绑定、参数类型、连接、通道冲突、非法范围和视觉替换兼容的静态校验入口
- [ ] 3.4 建立版本化Motion Core／Editor工具包清单和固定GUID发布流程，明确排除GF_X、AutoEra Adapter、玩法代码和美术资源
- [ ] 3.5 在ArtResource完成干净首次导入和同版本重复导入验证，确认合同、Rig与动作资产引用不丢失
- [ ] 3.6 记录Inspector工作流效率；只有证据证明不足时才另提可视化节点编辑器OpenSpec

## 4. P0-016 代表性纵向切片

- [ ] 4.1 用合同Builder生成轮式载体、四轮机构、多关节机械臂、可替换效应器、滑动门和传送带基础几何原型
- [ ] 4.2 验证四轮接地点、轮转、独立转向、反向转向、低速蟹行和有限悬挂表现，不实现完整车辆动力学
- [ ] 4.3 验证机械臂Yaw／Pitch／伸缩／腕部对齐、目标Pose、可达性、KeepOut、重新对位和安全收回
- [ ] 4.4 验证效应器对位、解锁、脱离、接入、锁定、取消、断电、安全保持和恢复序列
- [ ] 4.5 验证滑动门单／双扇开闭、安全占用、暂停／回弹和恢复；验证传送带循环、效率调速、堵塞保位和恢复
- [ ] 4.6 使用AutoEra Adapter接入代表性真实状态，并证明动作结束不触发结算、任务、导航或存档结果
- [ ] 4.7 验证对象池复用、重进场景、表现LOD恢复和连续两轮播放无残留或累积漂移

## 5. 三层验收与美术交付门

- [ ] 5.1 在固定测试面板为每项结构和动作能力提供播放、暂停、重置、中断、恢复及正／负例参数入口
- [ ] 5.2 建立独立基础几何演示场，以固定机位展示代表性动作、状态、净空、不可达和恢复结果
- [ ] 5.3 运行自动结构校验、确定性动作回归、Unity普通编译、Console检查和Profiler性能冒烟并记录证据
- [ ] 5.4 由用户完成基础几何纵向切片的最终可视验收；未通过前不得授权可动或关键装配正式模型生产
- [ ] 5.5 导出首批 `FunctionalRigContract` 与版本／指纹清单，交主美据此更新技术合同、轴测／三视图约束和模型任务包
- [ ] 5.6 使用一个合同兼容的替代视觉候选重跑结构、动作和可视测试，证明只替换视觉槽即可保持全部功能绑定
- [ ] 5.7 严格验证本OpenSpec、框架纯度、工具包边界和任务表只读约束，并将提交交给Git集成窗口使用中文信息提交且不推送
