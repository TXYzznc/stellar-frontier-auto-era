# B16 8092 重试实测结果

时间：2026-09-12 14:51–14:52 UTC。客户端3号仅使用正式项目 Unity REST 8092（PID 90340）；未调用 8090。

## 实测结果

- `AlgorithmServicesIntegrationTests`，job `92ab3c8e`：原生 `TestResults.xml` 新鲜结果 1/1 passed，start `14:51:22Z`，end `14:51:33Z`。覆盖真实 InitialRegion、导航目标/NavMesh 行进、作业服务生命周期，并断言关闭观察面板后导航仍 active；该测试未构造 `MotionWorkBridge` 或 `EffectorBehaviorQueue` 作业请求。
- `MotionWorkBridgeEditModeTests`，job `a87e9a31`：4/4 passed，0 failed。
- `MachineNavigationEditModeTests`，job `243cb8e3`：15/15 passed，0 failed。
- 编辑器结束态：`isPlaying=false`、`isPaused=false`、`isCompiling=false`；8092 health OK。Console 共 15 条 Log、0 Warning、0 Error。

## 未完成项

真实区域/NavMesh 与窗口关闭边界已有本次新鲜证据；真实 `MotionWorkBridge` 接入 `EffectorBehaviorQueue` 的作业预约/等待/安全取消/释放仍未覆盖，因为现有联合测试没有构造桥接或效应器队列。任务保持 Active，不登记本次为完整 result-ready。

## 资源

8092 保持可用；本次未修改源码、场景、Prefab、任务表、xlsx 或 Git。
